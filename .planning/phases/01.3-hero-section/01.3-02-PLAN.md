---
phase: 01.3
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/hero/HeroCtas.tsx
autonomous: true
requirements: [F1, NFR2, NFR3, NFR5]

must_haves:
  truths:
    - "View Projects" button renders as Button variant default, size lg, smooth-scrolls to #experience
    - "Contact Me" button renders as Button variant outline, size lg, smooth-scrolls to #contact
    - "Download CV" renders as a text link (not a third button) with Download icon, opens placeholder URL in new tab
    - All interactive elements are keyboard-focusable with visible focus rings
    - CTAs group at lg size with proper spacing (gap-4) between buttons
  artifacts:
    - path: src/components/hero/HeroCtas.tsx
      provides: CTA buttons group and Download CV text link
      exports: ["HeroCtas"]
  key_links:
    - from: HeroCtas.tsx
      to: src/components/ui/button.tsx
      via: "import { Button } from"
    - from: HeroCtas.tsx
      to: lucide-react
      via: "import { Download }"
---

<objective>
Create the HeroCtas component — CTA button row (View Projects + Contact Me) and the Download CV text link beneath.

Purpose: Satisfy D-09 (two lg buttons: default "View Projects" + outline "Contact Me"), D-10 (Download CV as text link, not a third button). Establishes the CTA group pattern for the hero section.

Output: src/components/hero/HeroCtas.tsx — exported HeroCtas component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/ui/button.tsx (Button component with default/outline variants, lg size)
@src/lib/utils.ts (cn utility)
@.planning/phases/01.3-hero-section/01.3-UI-SPEC.md (copywriting contract, Button Registry, interaction contracts)
@.planning/phases/01.3-hero-section/01.3-CONTEXT.md (D-09, D-10)

<interfaces>
From button.tsx:
```typescript
function Button({
  className,
  variant = "default",
  size = "default",
  ...props
}: ButtonPrimitive.Props & VariantProps<typeof buttonVariants>)

// variant: "default" | "outline" | "secondary" | "ghost" | "destructive" | "link"
// size: "default" | "xs" | "sm" | "lg" | "icon" | "icon-xs" | "icon-sm" | "icon-lg"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroCtas component</name>

  <read_first>
    /Users/minhpham/Claude/Projects/personal portfolio/src/components/ui/button.tsx (Button API: default/outline variants, lg size)
    /Users/minhpham/Claude/Projects/personal portfolio/.planning/phases/01.3-hero-section/01.3-UI-SPEC.md (Copywriting Contract: "View Projects" / "Contact Me" / "Download CV")
    /Users/minhpham/Claude/Projects/personal portfolio/.planning/phases/01.3-hero-section/01.3-CONTEXT.md (D-09: two lg buttons, D-10: Download CV text link)
  </read_first>

  <files>
    /Users/minhpham/Claude/Projects/personal portfolio/src/components/hero/HeroCtas.tsx
  </files>

  <action>
    Create `src/components/hero/HeroCtas.tsx`:

    ```typescript
    import { Download } from "lucide-react";
    import { Button } from "@/components/ui/button";

    export function HeroCtas() {
      return (
        <div className="flex flex-col items-start gap-4">
          <div className="flex flex-wrap gap-4">
            <Button
              variant="default"
              size="lg"
              asChild
            >
              <a href="#experience">View Projects</a>
            </Button>
            <Button
              variant="outline"
              size="lg"
              asChild
            >
              <a href="#contact">Contact Me</a>
            </Button>
          </div>
          <a
            href="#"
            target="_blank"
            rel="noopener noreferrer"
            className="group inline-flex items-center gap-1.5 text-sm text-muted-foreground transition-colors hover:text-accent focus-visible:outline-none focus-visible:underline"
          >
            <Download className="size-4 transition-transform group-hover:-translate-y-0.5 group-focus-visible:-translate-y-0.5" />
            Download CV
          </a>
        </div>
      );
    }
    ```

    Implementation details per UI-SPEC.md and CONTEXT.md:

    1. **D-09 — Two buttons at lg size:** "View Projects" uses `variant="default"` (primary filled button), "Contact Me" uses `variant="outline"` (bordered/transparent button). Both at `size="lg"` (h-9 px-2.5 per button.tsx lg size definition). Per UI-SPEC.md Copywriting Contract: exact text "View Projects" and "Contact Me".

    2. **Button asChild for anchor smooth scroll:** Both buttons use `asChild` prop with `<a>` child elements. This renders the button styling on a native anchor element, which supports `href="#experience"` for native smooth scrolling (via Lenis, already integrated in layout.tsx). Using `<a>` instead of `<button>` with `onClick` scroll handler avoids the `'use client'` requirement — these are plain anchor links.

    3. **D-10 — Download CV text link:** Rendered as a plain `<a>` element below the button row, NOT as a third Button component. Per UI-SPEC.md Copywriting Contract: "Download CV" text link with `Download` icon from lucide-react, opens in new tab (`target="_blank" rel="noopener noreferrer"`). Href is `"#"` as placeholder URL — user will provide the actual PDF link later. Uses `group` pattern: the icon lifts slightly on hover/focus-visible via `group-hover:-translate-y-0.5 group-focus-visible:-translate-y-0.5` (uses transform, compositor-friendly per web/performance.md).

    4. **Flex layout:** Outer div uses `flex-col items-start gap-4` (stacked below, left-aligned). Button row uses `flex-wrap gap-4` (side by side on desktop, wrap on mobile). Per UI-SPEC.md spacing scale: `gap-4` (lg = 24px) between buttons. Between CTA buttons and Download CV link, also `gap-4` via the outer flex-col gap.

    5. **Color:** Download CV link uses `text-muted-foreground` with `hover:text-accent` per UI-SPEC.md Color section ("Download CV text link color" in accent reserved list — accent is cyan). Focus-visible underline via `focus-visible:underline` for accessibility (NFR2).

    6. **No "use client":** This component is purely presentational with no hooks, state, or browser APIs. All interactions are native HTML anchor behavior.

    7. **Target sections per UI-SPEC.md Interaction Contracts:** "View Projects" scrolls to `#experience`, "Contact Me" scrolls to `#contact`. These match the section IDs in page.tsx (from Phase 1.2).
  </action>

  <verify>
    <automated>
      test -f /Users/minhpham/Claude/Projects/personal portfolio/src/components/hero/HeroCtas.tsx && echo "file: OK" || echo "file: MISSING"
      grep -q "export function HeroCtas" /Users/minhpham/Claude/Projects/personal portfolio/src/components/hero/HeroCtas.tsx && echo "export: OK" || echo "export: MISSING"
      grep -q 'variant="default"' /Users/minhpham/Claude/Projects/personal portfolio/src/components/hero/HeroCtas.tsx && echo "default variant: OK" || echo "default: MISSING"
      grep -q 'variant="outline"' /Users/minhpham/Claude/Projects/personal portfolio/src/components/hero/HeroCtas.tsx && echo "outline variant: OK" || echo "outline: MISSING"
      grep -q 'size="lg"' /Users/minhpham/Claude/Projects/personal portfolio/src/components/hero/HeroCtas.tsx && echo "lg size: OK" || echo "lg: MISSING"
      grep -q "View Projects" /Users/minhpham/Claude/Projects/personal portfolio/src/components/hero/HeroCtas.tsx && echo "View Projects text: OK" || echo "View Projects: MISSING"
      grep -q "Contact Me" /Users/minhpham/Claude/Projects/personal portfolio/src/components/hero/HeroCtas.tsx && echo "Contact Me text: OK" || echo "Contact Me: MISSING"
      grep -q "Download CV" /Users/minhpham/Claude/Projects/personal portfolio/src/components/hero/HeroCtas.tsx && echo "Download CV: OK" || echo "Download CV: MISSING"
      grep -q "lucide-react" /Users/minhpham/Claude/Projects/personal portfolio/src/components/hero/HeroCtas.tsx && echo "lucide import: OK" || echo "lucide: MISSING"
      grep -q 'href="#experience"' /Users/minhpham/Claude/Projects/personal portfolio/src/components/hero/HeroCtas.tsx && echo "experience anchor: OK" || echo "experience: MISSING"
      grep -q 'href="#contact"' /Users/minhpham/Claude/Projects/personal portfolio/src/components/hero/HeroCtas.tsx && echo "contact anchor: OK" || echo "contact: MISSING"
      grep -q 'target="_blank"' /Users/minhpham/Claude/Projects/personal portfolio/src/components/hero/HeroCtas.tsx && echo "blank target: OK" || echo "blank target: MISSING"
      grep -q "asChild" /Users/minhpham/Claude/Projects/personal portfolio/src/components/hero/HeroCtas.tsx && echo "asChild: OK" || echo "asChild: MISSING"
      npx tsc --noEmit --pretty false 2>&1 | grep "^src/components/hero/HeroCtas" && echo "TSC ERROR" || echo "tsc: OK"
    </automated>
  </verify>

  <acceptance_criteria>
    - `src/components/hero/HeroCtas.tsx` exists and exports `HeroCtas` as named export
    - "View Projects" rendered as Button `variant="default" size="lg" asChild` with `<a href="#experience">`
    - "Contact Me" rendered as Button `variant="outline" size="lg" asChild` with `<a href="#contact">`
    - Both buttons use `asChild` with native `<a>` elements for Lenis-compatible smooth scroll
    - "Download CV" rendered as plain `<a>` with `target="_blank"`, `Download` icon from lucide-react, `text-muted-foreground hover:text-accent`
    - Download CV has `href="#"` (placeholder URL)
    - Button row uses `flex-wrap gap-4`, outer container uses `flex-col items-start gap-4`
    - No `"use client"` directive (static component)
    - TypeScript compiles without errors: `npx tsc --noEmit --pretty false`
  </acceptance_criteria>

  <done>
    HeroCtas created: "View Projects" (default lg) + "Contact Me" (outline lg) buttons with native anchor smooth scroll, "Download CV" text link with Download icon and placeholder URL.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static content (no user input) | No trust boundary issues — static navigation links only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.3-02 | Information Disclosure | CV download link | accept | Placeholder URL (`href="#"`) contains no actual document path. User will provide real PDF URL later. |
</threat_model>

<verification>
1. `npx tsc --noEmit --pretty false` shows no errors for `src/components/hero/HeroCtas.tsx`
2. All grep commands from task verify section pass
3. Button variants and sizes match available options in shadcn Button component
</verification>

<success_criteria>
- "View Projects" rendered as Button default lg, smooth-scrolls anchor to #experience
- "Contact Me" rendered as Button outline lg, smooth-scrolls anchor to #contact
- "Download CV" rendered as text link with Download icon and target="_blank"
- Buttons use asChild with native <a> elements for compatibility with Lenis scroll
- No "use client" directive — purely static component
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/01.3-hero-section/01.3-02-SUMMARY.md`
</output>
