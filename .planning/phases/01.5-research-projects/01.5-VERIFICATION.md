---
phase: 01.5-research-projects
verified: 2026-06-21T22:00:00Z
status: human_needed
score: 16/18 truths verified
behavior_unverified: 2
overrides_applied: 0
behavior_unverified_items:
  - truth: "Crossfade animation applies opacity transition on filter change"
    test: "Click a non-active filter tab, observe grid opacity transition before URL updates"
    expected: "Grid fades from opacity-100 to opacity-0, then new filtered content fades in at opacity-100"
    why_human: "Presence checks confirm the CSS transition classes (opacity-0, duration-150, duration-200, ease-out) and state management (transitioning useState, setTimeout navigation, useEffect status-change detection) are wired. But the actual animation timing and visual outcome require browser interaction to verify — no automated test exercises this path."
  - truth: "prefers-reduced-motion disables crossfade animation"
    test: "Enable reduced motion in OS/browser settings, then click a filter tab"
    expected: "Grid instantly switches to new filter without opacity transition (isTransitioning is forced false)"
    why_human: "window.matchMedia('(prefers-reduced-motion: reduce)') is a runtime browser API that cannot be evaluated statically. The code path exists (lines 79-84 in ResearchGrid.tsx: typeof window check, matchMedia call, isTransitioning guard) but no test exercises it."
human_verification:
  - test: "Click a non-active filter tab, observe grid opacity transition before URL updates"
    expected: "Grid fades from opacity-100 to opacity-0, then new filtered content fades in at opacity-100"
    why_human: "Crossfade animation uses opacity transitions with 150ms exit / 200ms enter timing. Presence checks confirm code is wired (transitioning state, setTimeout navigation, useEffect status-change detection, CSS classes). Actual animation requires browser interaction."
  - test: "Enable reduced motion in OS/browser settings, then click a filter tab"
    expected: "Grid instantly switches to new filter without opacity transition"
    why_human: "window.matchMedia('(prefers-reduced-motion: reduce)') is a runtime browser API. Presence checks confirm the guard exists (lines 79-84 in ResearchGrid.tsx). Behavior requires user testing."
---

# Phase 1.5: Research Projects Verification Report

**Phase Goal:** Build the Research Projects section — data layer (Zod schema + 5 sample projects), three core components (PaperLinkChip, FilterTabs, ResearchCard), and the ResearchGrid orchestrator with Suspense, crossfade animation, and URL-driven filtering — wired into the home page after Experience.

**Verified:** 2026-06-21T22:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Research data has 5+ projects with all D-14 fields, at least 1 featured | VERIFIED | `src/app/data/research.ts` — 5 projects, 1 featured (line 49 `featured: true`), all 14 fields present across projects |
| 2 | Status field uses Zod enum (in-progress/submitted/published/accepted) | VERIFIED | `src/app/data/research.ts` line 5-10 — `ProjectStatusEnum = z.enum(["in-progress","submitted","published","accepted"])` |
| 3 | Badge component accepts optional statusColor prop matching all 4 statuses | VERIFIED | `src/components/ui/badge.tsx` line 23-32 — CVA `statusColor` variant with all 4 keys; line 43 destructured from props; line 51 passed to `badgeVariants()` |
| 4 | Existing Badge usage (Experience section) unbroken by the extension | VERIFIED | `src/components/experience/experience-entry.tsx` line 52 uses `<Badge variant="secondary">` (no statusColor). Build exits 0. |
| 5 | getAvailableStatuses() utility derives unique statuses from data | VERIFIED | `src/app/data/research.ts` line 119-122 — `Set`-based derivation from `researchProjects.map(p => p.status)` |
| 6 | PaperLinkChip renders icon + label for arxiv/doi/pdf links, opens in new tab | VERIFIED | `src/components/research/PaperLinkChip.tsx` line 19-28 — `<a target="_blank" rel="noopener noreferrer">`, chipConfig maps type to icon+label |
| 7 | FilterTabs shows 'All' + auto-detected status tabs from data | VERIFIED | `src/components/research/FilterTabs.tsx` line 24 — `tabs: string[] = ["all", ...availableStatuses]` |
| 8 | FilterTabs uses role=tablist + role=tab with aria-selected | VERIFIED | `src/components/research/FilterTabs.tsx` line 28 `role="tablist"`, line 33 `role="tab"`, line 36 `aria-selected={activeStatus === status}` |
| 9 | ResearchCard renders title, status badge, abstract (clamp+expand), tags, paper links | VERIFIED | `src/components/research/ResearchCard.tsx` lines 37-113 — Card compound with all elements |
| 10 | Featured card has visual accent treatment | VERIFIED | `src/components/research/ResearchCard.tsx` line 40-41 `"md:col-span-2 border-t-2 border-accent"`, line 47 `"text-xl"` title |
| 11 | Abstract expand toggle only appears when abstract > 150 chars | VERIFIED | `src/components/research/ResearchCard.tsx` line 78 — `{project.abstract.length > 150 && (...)}` |
| 12 | ResearchGrid renders filter tabs, bento card grid, and empty state | VERIFIED | `src/components/research/ResearchGrid.tsx` lines 86-126 — FilterTabs at top, grid-cols-1 md:grid-cols-2, empty state block |
| 13 | useSearchParams wrapped in Suspense boundary (build does not fail) | VERIFIED | `src/components/research/ResearchGrid.tsx` line 134 `<Suspense>` wraps `ResearchGridInner`. Build exits 0. |
| 14 | Crossfade animation applies opacity transition on filter change | PRESENT_BEHAVIOR_UNVERIFIED | Code present: transitioning state (line 48), setTimeout navigation with 150ms (line 58-60), useEffect status detection (line 72-77), conditional opacity classes (line 97-99). No test exercises the runtime animation path. |
| 15 | Empty state shows 'No {status} projects yet' + 'Show all' button when filtered set is empty | VERIFIED | `src/components/research/ResearchGrid.tsx` lines 107-122 — conditional message + "Show all" Button when `activeStatus !== "all"` |
| 16 | Research section renders on the home page after Experience | VERIFIED | `src/app/page.tsx` lines 20-26 — `<Section id="research">` between Experience (line 18) and Skills (line 28) |
| 17 | Featured card spans 2 columns on desktop (md:col-span-2) | VERIFIED | `src/components/research/ResearchCard.tsx` line 40-41 — `md:col-span-2` in featured conditional |
| 18 | prefers-reduced-motion disables crossfade animation | PRESENT_BEHAVIOR_UNVERIFIED | Code present: `window.matchMedia("(prefers-reduced-motion: reduce)")` check (line 80-81), `isTransitioning` guard (line 84). No test exercises the runtime media query evaluation. |

**Score:** 16/18 truths verified (2 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/app/data/research.ts` | Zod schema + typed data + utility | VERIFIED | 122 lines, exports ResearchProjectSchema, ResearchProject, researchProjects (5), getAvailableStatuses |
| `src/components/ui/badge.tsx` | Extended CVA with statusColor | VERIFIED | 63 lines, statusColor variants for all 4 statuses, optional prop, existing usage unbroken |
| `src/components/research/PaperLinkChip.tsx` | Icon+label link chip | VERIFIED | 29 lines, ExternalLink for arxiv/doi, FileText for pdf, target="_blank" noopener |
| `src/components/research/FilterTabs.tsx` | URL-driven filter tabs | VERIFIED | 50 lines, role=tablist, role=tab, aria-selected, auto-detected tabs |
| `src/components/research/ResearchCard.tsx` | Project card with all features | VERIFIED | 115 lines, Card+Badge+PaperLinkChip composition, featured accent, abstract expand |
| `src/components/research/ResearchGrid.tsx` | Grid orchestrator with Suspense | VERIFIED | 144 lines, outer Suspense wrapper + inner useSearchParams client, crossfade, empty state |
| `src/app/page.tsx` | Research section after Experience | VERIFIED | 45 lines, imports ResearchGrid, Section id="research" with subtitle |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| ResearchGrid | research.ts | `import { researchProjects, getAvailableStatuses }` | WIRED | Line 10 in ResearchGrid.tsx |
| ResearchGrid | FilterTabs | `import { FilterTabs }` + `onFilterChange` callback | WIRED | Lines 7, 88-92 in ResearchGrid.tsx |
| ResearchGrid | ResearchCard | `import { ResearchCard }` + maps filtered projects | WIRED | Lines 8, 103 in ResearchGrid.tsx |
| ResearchGrid | Button | `import { Button }` + empty state "Show all" | WIRED | Lines 9, 115 in ResearchGrid.tsx |
| ResearchCard | research.ts | `import type { ResearchProject }` | WIRED | Line 17 in ResearchCard.tsx |
| ResearchCard | Badge | `import { Badge }` + `statusColor` prop | WIRED | Lines 5, 50 in ResearchCard.tsx |
| ResearchCard | PaperLinkChip | `import { PaperLinkChip }` + conditional render | WIRED | Lines 16, 102-110 in ResearchCard.tsx |
| Badge | Experience section | `<Badge variant="secondary">` (no statusColor) | WIRED | experience-entry.tsx line 52, unchanged |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| ResearchGrid | `activeStatus` | URL searchParams via `useSearchParams().get("status")` | Dynamic URL param | FLOWING |
| ResearchGrid | `filteredProjects` | Computed from `researchProjects` (static import) filtered by `activeStatus` | Static import (compile-time), filter logic dynamic | FLOWING |
| ResearchGrid | `availableStatuses` | `getAvailableStatuses()` reading static `researchProjects` | Static data | FLOWING |
| ResearchCard | `project` | Props passed from ResearchGrid map | Real ResearchProject objects | FLOWING |

All data flows trace to the compile-time `researchProjects` array or URL params. No disconnected props or hardcoded empty values found.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| TypeScript compilation + static export | `npm run build` | Exit 0, static export to `./out/` | PASS |
| Research data module validates | Module import check via build | 5 projects, all 4 statuses, 1 featured | PASS |

### Probe Execution

SKIPPED — no probes discovered in phase PLANs or conventional paths (`scripts/*/tests/probe-*.sh`).

### Requirements Coverage

| Requirement | Plan(s) | Description | Status | Evidence |
| ----------- | ------- | ----------- | ------ | -------- |
| F4-01 | 01, 03 | Research project card grid with bento layout | SATISFIED | ResearchGrid.tsx grid-cols-1 md:grid-cols-2 + ResearchCard.tsx md:col-span-2 for featured |
| F4-02 | 01, 02 | Status badges (in-progress/submitted/published/accepted) | SATISFIED | badge.tsx statusColor CVA + ResearchCard Badge usage |
| F4-03 | 03 | Filter tabs using URL search params | SATISFIED | FilterTabs.tsx + ResearchGrid.tsx useSearchParams/useRouter |
| F4-04 | 02 | PDF/DOI/arXiv paper link chips | SATISFIED | PaperLinkChip.tsx for all 3 link types |
| F4-05 | 02 | Abstract truncation with expand/collapse | SATISFIED | ResearchCard.tsx line-clamp-3 + useState toggle + length check |
| F4-06 | 03 | Crossfade animation on filter change | PRESENT_BEHAVIOR_UNVERIFIED | Code wired (CSS opacity transitions + state management), behavior not test-verified |
| F4-07 | 03 | Empty state with reset button | SATISFIED | ResearchGrid.tsx "No {status} projects yet" + "Show all" Button |

All 7 Phase 1.5 requirement IDs accounted for. No orphaned requirements.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| None | - | - | - | - |

No debt markers (TBD/FIXME/XXX), no console.log, no placeholder comments, no empty implementations, no stub patterns found across all 7 verified files.

### Human Verification Required

2 items need human testing (behavior-unverified truths — code wired, runtime behavior not exercised by a test):

**1. Crossfade animation on filter change**

**Test:** Click a non-active filter tab (e.g., switch from "All" to "Published") on the home page.
**Expected:** Grid content fades out (opacity transitions to 0 over 150ms), then the URL updates and new filtered content fades in (opacity transitions to 100 over 200ms). No clipping or visual glitches on the featured card (which spans 2 columns).
**Why human:** The code is present and wired — `transitioning` state (ResearchGrid.tsx line 48), `setTimeout` navigation (line 58-60), `useEffect` status-change detection (line 72-77), conditional CSS classes (line 97-99) — but the actual animation timing and visual smoothness require browser interaction to verify.

**2. prefers-reduced-motion disables crossfade**

**Test:** Enable "Reduce motion" in OS accessibility settings (macOS: System Settings > Accessibility > Display > Reduce motion). Then click a filter tab on the home page.
**Expected:** Grid switches to the new filter instantly without any opacity transition.
**Why human:** `window.matchMedia("(prefers-reduced-motion: reduce)")` is a runtime browser API. The code path exists (ResearchGrid.tsx lines 79-84: `typeof window` guard, `matchMedia` call, `isTransitioning = transitioning && !prefersReducedMotion`) but no test can evaluate this statically.

### Gaps Summary

No gaps found. All 7 required artifacts exist, are substantive, wired, and data flows through them. All 7 requirement IDs are satisfied (6 fully verified, 1 behavior-unverified). 16 of 18 truths are fully verified; 2 are present and wired but behavior-unverified (crossfade animation + reduced-motion guard) — these route to human verification rather than blocking.

---

_Verified: 2026-06-21T22:00:00Z_
_Verifier: Claude (gsd-verifier)_
