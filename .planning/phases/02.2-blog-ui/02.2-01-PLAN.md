---
phase: 02.2-blog-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/blog/page.tsx
  - src/components/blog/blog-hero-card.tsx
  - src/components/blog/blog-card.tsx
  - src/components/blog/tag-filter.tsx
  - src/components/blog/blog-list.tsx
autonomous: true
requirements: [F6]
user_setup: []
must_haves:
  truths:
    - "User sees a blog list page at /blog with page heading and tag pills"
    - "Latest post renders as a full-width hero card with cover image or gradient fallback"
    - "Remaining posts render in a responsive 2-column (desktop) / 1-column (mobile) grid"
    - "Clicking a tag pill filters posts client-side and updates URL to ?tag=xyz"
    - "Clicking 'All' tag clears the filter and shows all posts"
    - "Hero card and grid cards link to /blog/[slug]"
    - "Grid cards show cover thumbnail, title, excerpt (2-line clamp), date, and tags"
    - "Empty filter state shows 'No posts found' message"
  artifacts:
    - path: src/components/blog/blog-hero-card.tsx
      provides: "Hero card for latest post with cover image / gradient fallback, title, excerpt, metadata, tags"
      min_lines: 50
    - path: src/components/blog/blog-card.tsx
      provides: "Grid card for non-hero posts, reuses ui/card compound component"
      min_lines: 40
    - path: src/components/blog/tag-filter.tsx
      provides: "Tag pill row with URL search param integration, active/inactive states"
      min_lines: 40
    - path: src/components/blog/blog-list.tsx
      provides: "Composition orchestrator wiring hero, grid, tag filter, empty states"
      min_lines: 60
    - path: src/app/blog/page.tsx
      provides: "Blog list route with static data loading and Suspense boundary"
      min_lines: 25
  key_links:
    - from: src/app/blog/page.tsx
      to: src/app/data/blog.ts
      via: "calls getAllPosts() and getAllTags() at build time"
      pattern: "getAll(Posts|Tags)\\(\\)"
    - from: src/components/blog/blog-list.tsx
      to: src/components/blog/tag-filter.tsx
      via: "renders TagFilter and passes active tag state"
    - from: src/components/blog/blog-list.tsx
      to: src/components/blog/blog-hero-card.tsx
      via: "renders latest post as BlogHeroCard"
    - from: src/components/blog/blog-list.tsx
      to: src/components/blog/blog-card.tsx
      via: "maps remaining posts to BlogCard in grid"
    - from: src/components/blog/tag-filter.tsx
      to: next/navigation
      via: "useSearchParams / useRouter for URL-based tag filtering"
---

<objective>
Create the blog list page at `/blog` with hero + grid card layout and tag filtering.

Purpose: Deliver the primary blog surface — users browse posts, scan by tag, and navigate to individual posts.

Output: A new blog route at /blog with hero card, grid cards, tag filter pills, and empty/error states.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02.2-blog-ui/02.2-CONTEXT.md
@.planning/phases/02.2-blog-ui/02.2-UI-SPEC.md
@src/app/data/blog.ts
@src/components/ui/card.tsx
@src/components/ui/badge.tsx
@src/components/research/FilterTabs.tsx
@src/app/page.tsx
@src/app/blog/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: BlogHeroCard and BlogCard components</name>
  <files>
    src/components/blog/blog-hero-card.tsx
    src/components/blog/blog-card.tsx
  </files>
  <action>
    Create two presentation components under src/components/blog/. Neither requires "use client" — all interactivity is CSS-driven via Tailwind group hover patterns and Link from next/link.

    **BlogHeroCard** (src/components/blog/blog-hero-card.tsx):
    - Props: post of type BlogPostMeta (imported from @/app/data/blog)
    - Renders a Link wrapping the entire card, pointing to /blog/{slug}
    - Card background: if post.cover exists, render an img with object-fit cover and a dark gradient overlay for text readability; if no cover, render a div with the --gradient-hero CSS gradient (cyan-to-violet 135deg, same as hero section)
    - Max-height 480px on desktop, auto on mobile — use Tailwind max-h-[480px] md:max-h-[480px]
    - Content overlay at bottom: title in Display token (text-2xl md:text-[28px] font-semibold leading-tight), excerpt in Body (text-base text-muted-foreground, line-clamp-3), metadata row with date + reading time (text-sm text-muted-foreground), tags rendered as Badge component with variant="outline"
    - Hover: group-hover:scale-[1.01] shadow-md to shadow-lg transition with border accent via group-hover:border-accent/50 — use a wrapping div with rounded-xl overflow-hidden
    - Focus-visible: ring-2 ring-accent
    - Active: group-active:scale-[0.99]
    - Duration: 200ms ease-out for all transitions

    **BlogCard** (src/components/blog/blog-card.tsx):
    - Props: post of type BlogPostMeta
    - Wraps content in a Link to /blog/{slug}
    - Uses the existing Card compound component (from @/components/ui/card) with size="default"
    - Cover thumbnail area: 16:9 aspect-ratio container with object-fit cover img, or gradient fallback div if no cover
    - CardHeader: title in Heading token (text-[20px]/[1.3] font-semibold)
    - CardDescription: excerpt in Body (text-base, line-clamp-2)
    - CardFooter: date + tags (text-sm text-muted-foreground for date, Badge with variant="outline" for tags)
    - Hover: shadow-sm to shadow-md, -translate-y-0.5, border border-accent/50 via group-hover
    - Focus-visible: ring-2 ring-accent
    - Active: group-active:scale-[0.99]
    - Use group/card for Tailwind group-hover behavior
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    BlogHeroCard and BlogCard both compile cleanly, render post data with cover/gradient fallback, and link to /blog/[slug].
  </done>
</task>

<task type="auto">
  <name>Task 2: TagFilter client component</name>
  <files>
    src/components/blog/tag-filter.tsx
  </files>
  <action>
    Create a client component ("use client") for tag filtering via URL search params.

    **Props:**
    - tags: string[] — all available tags from getAllTags()
    - className?: string

    **Behavior:**
    - Use useSearchParams and useRouter from next/navigation to read/write ?tag=
    - Render a row of pill buttons: first is "All" (clears param), rest are tags
    - Each pill is a Badge component (from @/components/ui/badge):
      - Active (tag matches search param): variant="default" with additional bg-accent/10 text-accent treatment
      - Inactive: variant="outline"
    - Clicking a tag calls router.push(`?tag=${tag}`, { scroll: false }) — use router.replace for cleaner history
    - Clicking "All" calls router.push(``) to clear the tag param, or router.push(window.location.pathname)
    - Wrapped in a div with role="tablist" and aria-label for accessibility
    - Responsive: flex-wrap on desktop, overflow-x-auto with hidden scrollbar on mobile (use Tailwind scrollbar-hide)
    - Active tag should have a subtle accent-colored left border or background to distinguish from inactive
    - Transition: 150ms ease color/bg transitions on all pills

    **Constraint:** This component uses useSearchParams, so its parent must wrap it in Suspense. Document this in a comment at the top of the file.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TagFilter compiles, renders tag pills with active/inactive states, and updates URL params on click.
  </done>
</task>

<task type="auto">
  <name>Task 3: BlogList composition and blog route page</name>
  <files>
    src/components/blog/blog-list.tsx
    src/app/blog/page.tsx
  </files>
  <action>
    Create BlogList orchestrator and the /blog page route.

    **BlogList** (src/components/blog/blog-list.tsx):
    - Props: posts (BlogPostMeta[]), tags (string[])
    - "use client" component that wraps useSearchParams for tag filtering
    - If activeTag is set, filter posts where post.tags includes activeTag
    - Split filtered posts: first one is the hero, rest go in grid
    - Layout (per UI-SPEC):
      - Page heading "Blog" at Display size (text-[28px] font-semibold tracking-tight)
      - TagFilter row below heading
      - BlogHeroCard (first post, full width)
      - Grid section (remaining posts): grid grid-cols-1 md:grid-cols-2 gap-6
      - If 0 posts after filtering: render empty state with "No posts found" heading and "No posts match the selected tag. Try a different filter." body (from UI-SPEC copywriting contract)
      - If no posts at all (empty getAllPosts): render "No posts yet" + "Blog posts will appear here once published. Check back soon."
    - Transition on filter change: wrap the post grid in a keyed div (key=activeTag) with a CSS animation (opacity 0->1 over 300ms using animate-in fade-in)
    - Import TagFilter with a Suspense boundary wrapping it (since useSearchParams requires it)

    **Blog Page** (src/app/blog/page.tsx):
    - Server component, no "use client"
    - Import getAllPosts() and getAllTags() from @/app/data/blog
    - These are build-time synchronous calls (fs-based)
    - Wrap BlogList in a Suspense boundary with a minimal centered spinner or skeleton
    - Use a Container component (imported from @/components/container) with maxWidth="xl" for consistent page width
    - Add padding top: py-12 or pt-24 (to account for fixed header)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    BlogList compiles, renders with hero + grid + tags, filters posts by URL tag param, shows empty states.
    Blog page at /blog renders with static generation, no runtime errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static export → browser | All content is build-time generated. No dynamic user input enters this page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.2-01 | Tampering | Tag filter URL params | accept | Static export, no database or backend. URL param manipulation only affects client-side filtering, no persistence. |
| T-2.2-02 | Information Disclosure | Blog post data | accept | All post content is public by design. No private data in blog frontmatter or content. |
| T-2.2-SC | Tampering | npm package installs | mitigate | No new package installs in this plan. All packages (next, react, tailwind, lucide) already installed in Phase 2.1. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for all new files
- `npm run build` succeeds with static export generating /blog/index.html
- Generated pages contain: hero card with title/excerpt/gradient fallback, grid cards, tag pills, filtered states
</verification>

<success_criteria>
1. BlogHeroCard and BlogCard render with correct typography, spacing, and hover states per UI-SPEC
2. TagFilter pills show active/inactive states and update URL params
3. BlogList filters posts client-side when ?tag= param is set
4. Empty filter state displays copywritten "No posts found" message
5. Full static export build succeeds with no errors
</success_criteria>

<output>
Create .planning/phases/02.2-blog-ui/02.2-01-SUMMARY.md when done
</output>
