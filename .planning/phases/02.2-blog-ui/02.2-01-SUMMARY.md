---
phase: 02.2-blog-ui
plan: 01
subsystem: blog
tags:
  - blog-list
  - hero-card
  - grid-cards
  - tag-filter
  - url-params
dependency_graph:
  requires:
    - Phase 2.1 — blog data model, MDX loaders, blog/[slug] route
  provides:
    - Blog list page at /blog with hero+grid layout
    - Tag filtering via URL search params (?tag=)
  affects:
    - Phase 2.2-02: blog post page polish (breadcrumb, cover, TOC)
    - Phase 2.2-03: tag route pages, series nav, share buttons
tech-stack:
  added: []
  patterns:
    - Client-side URL param filtering with useSearchParams/useRouter
    - Server component + client boundary pattern (Suspense wrapping useSearchParams consumers)
    - Group-hover card interactions with Tailwind composition
key-files:
  created:
    - src/components/blog/blog-hero-card.tsx
    - src/components/blog/blog-card.tsx
    - src/components/blog/tag-filter.tsx
    - src/components/blog/blog-list.tsx
    - src/app/blog/page.tsx
  modified: []
decisions:
  - TagFilter uses router.replace (not router.push) for cleaner history when filtering
  - BlogList wraps TagFilter in Suspense at page level for static export compatibility
  - Hero card renders inline img tag (not next/image) — consistent with static export (images.unoptimized)
  - Card compound component reused for BlogCard; CardFooter provides the metadata row
metrics:
  duration: ~45 min
  completed_date: "2026-06-23"
  tasks_completed: 4
  files_created: 5
status: complete
---

# Phase 2.2 Plan 01: Blog List Page

Blog list page at /blog with hero card, grid cards, tag filtering, and empty states.

## Completed Tasks

| Task | Name                          | Commit | Files |
| ---- | ----------------------------- | ------ | ----- |
| 1    | BlogHeroCard and BlogCard     | d6980d8 | blog-hero-card.tsx, blog-card.tsx |
| 2    | TagFilter client component    | cd20236 | tag-filter.tsx |
| 3    | BlogList + blog route page    | cbe034c | blog-list.tsx, page.tsx |

## Deviations from Plan

None — plan executed as written.

## Threat Flags

None — no new security-relevant surface introduced. All content is build-time generated with client-side filtering via URL params.

## Known Stubs

None.

## Verification Results

- `npx tsc --noEmit` passes clean (0 errors)
- `npm run build` succeeds with static export
- Generated output includes `out/blog.html` and `out/blog/hello-world.html`
- Route at /blog renders with serialized post data, Suspense boundary for client-side hydration

## Self-Check: PASSED

All files verified to exist on disk and commits confirmed in git log.
