---
phase: 11-project-scaffold-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - "Clean boilerplate"
  - "Set up Tailwind v4 theme"
  - "Define CSS custom properties"
  - "Configure fonts"
  - "Set up shadcn/ui customization"
must_haves:
  truths:
    - "Page loads with Inter font, not Geist"
    - "CSS custom properties exist for accent (cyan-teal), accent-secondary (violet), gradient-hero"
    - "Tailwind utilities bg-accent, text-accent, text-accent-secondary are usable"
    - "No flash of light theme on page load when dark mode is preferred"
    - "Stock create-next-app boilerplate is removed"
    - "JetBrains Mono is available as the mono/code font"
    - "@theme inline block references Inter and JetBrains Mono correctly"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Design tokens, accent colors, gradient animation keyframes, updated font variables"
      contains: "--accent: oklch(0.65 0.18 210)"
    - path: "src/app/layout.tsx"
      provides: "Font config, flash prevention inline script, metadata"
      contains: "JetBrains_Mono"
    - path: "src/app/page.tsx"
      provides: "Clean portfolio skeleton without stock Next.js boilerplate"
      does_not_contain: "get-started"
  key_links:
    - from: "layout.tsx"
      to: "globals.css"
      via: "font CSS variables (--font-inter, --font-jetbrains-mono)"
      pattern: "inter.variable"
    - from: "layout.tsx <head>"
      to: "document.documentElement.classList"
      via: "inline blocking script for flash prevention"
      pattern: "localStorage.getItem"
---

<objective>
Replace the create-next-app default stack with the portfolio design system foundation.

Purpose: Establish the brand identity (Inter + JetBrains Mono fonts, cyan-teal/violet accent palette, dark mode default), configure CSS custom properties as Tailwind utilities, and prevent theme flash on page load. This is the foundation that all subsequent phases build on.

Output: Updated globals.css, layout.tsx, and page.tsx with clean design system tokens and no boilerplate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-project-scaffold-design-system/11-CONTEXT.md
@.planning/phases/11-project-scaffold-design-system/11-RESEARCH.md
@.planning/phases/11-project-scaffold-design-system/11-PATTERNS.md
@.planning/phases/11-project-scaffold-design-system/11-UI-SPEC.md

<interfaces>
**Current globals.css** (self-analog — read before modifying):
- Uses `@theme inline` block which creates Tailwind utilities from CSS variables
- Currently has `--font-sans: var(--font-sans)` (self-referencing), `--font-mono: var(--font-geist-mono)`, `--font-heading: var(--font-sans)`
- `:root` and `.dark` blocks hold shadcn CSS variables
- `@custom-variant dark (&:is(.dark *))` enables dark mode utility variants
- Keyframes must be defined for gradient animation to be used by HeroBackground in Plan 02

**Current layout.tsx** (self-analog — read before rewriting):
- Imports `Geist` and `Geist_Mono` from `next/font/google`
- Sets `--font-geist-sans` and `--font-geist-mono` CSS variables
- Metadata title is "Create Next App"
- `<body>` has `min-h-full flex flex-col` classes

**Font type signatures from next/font/google:**
```typescript
// Inter is a variable font (wght axis)
const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

// JetBrains Mono is a variable font (wght axis)
const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-jetbrains-mono",
  display: "swap",
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update globals.css with design tokens, accent colors, and gradient keyframes</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (current content)
    - .planning/phases/11-project-scaffold-design-system/11-RESEARCH.md (lines 229-256 for @theme inline pattern, lines 265-297 for gradient animation)
    - .planning/phases/11-project-scaffold-design-system/11-PATTERNS.md (lines 267-310 for exact additions)
    - .planning/phases/11-project-scaffold-design-system/11-UI-SPEC.md (lines 78-106 for exact color values)
  </read_first>
  <action>
    Edit `src/app/globals.css` with these EXACT changes:

    **1. Update `@theme inline` block:**
    Change existing entries and add new ones:

    ```
    --font-sans: var(--font-inter);
    --font-mono: var(--font-jetbrains-mono);
    --font-heading: var(--font-inter);
    --color-accent: var(--accent);
    --color-accent-foreground: var(--accent-foreground);
    --color-accent-secondary: var(--accent-secondary);
    ```

    These replace: `--font-sans: var(--font-sans)` with `--font-sans: var(--font-inter)`, `--font-mono: var(--font-geist-mono)` with `--font-mono: var(--font-jetbrains-mono)`, and add three new color tokens. `--font-heading` changes from `var(--font-sans)` to `var(--font-inter)` (same resolved value, clearer source).

    Also add the gradient-shift animation and its keyframe definition inside @theme inline:
    ```
    --animate-gradient-shift: gradient-shift 20s ease infinite;
    @keyframes gradient-shift {
      0%, 100% { background-position: 0% 50%; }
      25% { background-position: 100% 0%; }
      50% { background-position: 100% 100%; }
      75% { background-position: 0% 100%; }
    }
    ```

    **2. Add to `:root` block** (after existing variables):
    ```
    --accent: oklch(0.65 0.18 210);
    --accent-foreground: oklch(0.98 0 0);
    --accent-secondary: oklch(0.55 0.15 280);
    --gradient-hero: linear-gradient(135deg, oklch(0.65 0.18 210), oklch(0.55 0.15 280));
    ```

    **3. Add to `.dark` block** (after existing variables):
    ```
    --accent: oklch(0.65 0.18 210);
    --accent-foreground: oklch(0.98 0 0);
    --accent-secondary: oklch(0.6 0.18 280);
    ```

    **4. Do NOT** add `--gradient-hero` to `@theme inline` — `linear-gradient()` values cannot be used in Tailwind `bg-*` utilities (per RESEARCH.md Assumption A4). The gradient token is used directly as `background: var(--gradient-hero)` in inline styles.

    **DO NOT** change any existing shadcn values (--background, --foreground, --card, --primary, etc.). Only add the new accent tokens and update font variable references.
  </action>
  <verify>
    <automated>
      grep -q '--accent: oklch(0.65 0.18 210)' src/app/globals.css &&
      grep -q '--color-accent: var(--accent)' src/app/globals.css &&
      grep -q '--font-inter' src/app/globals.css &&
      grep -q '--font-jetbrains-mono' src/app/globals.css &&
      grep -q '@keyframes gradient-shift' src/app/globals.css &&
      grep -q '--gradient-hero' src/app/globals.css
    </automated>
  </verify>
  <done>
    All accent tokens defined in :root and .dark. @theme inline updated with new color references and font variables. Gradient keyframes defined. No existing shadcn tokens removed or altered.
  </done>
  <acceptance_criteria>
    - `grep -c '--accent:' src/app/globals.css` returns 3 (one in :root, one in .dark, one via --color-accent in @theme)
    - `grep '--font-sans:' src/app/globals.css` contains `var(--font-inter)` not `var(--font-sans)`
    - `grep '--font-mono:' src/app/globals.css` contains `var(--font-jetbrains-mono)` not `var(--font-geist-mono)`
    - `grep '@keyframes gradient-shift' src/app/globals.css` exits 0
    - `grep '--gradient-hero' src/app/globals.css` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewrite layout.tsx with Inter + JetBrains Mono fonts and flash prevention script</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (current content — must read before writing)
    - .planning/phases/11-project-scaffold-design-system/11-RESEARCH.md (lines 434-483 for exact pattern)
    - .planning/phases/11-project-scaffold-design-system/11-PATTERNS.md (lines 193-264 for specific replacements)
  </read_first>
  <action>
    Replace the entire content of `src/app/layout.tsx` with the following:

    ```typescript
    import type { Metadata } from "next";
    import { Inter, JetBrains_Mono } from "next/font/google";
    import "./globals.css";

    const inter = Inter({
      subsets: ["latin"],
      variable: "--font-inter",
      display: "swap",
    });

    const jetbrainsMono = JetBrains_Mono({
      subsets: ["latin"],
      variable: "--font-jetbrains-mono",
      display: "swap",
    });

    export const metadata: Metadata = {
      title: "Tony Portfolio",
      description: "AI Engineer & Researcher",
    };

    const themeScript = `
      (function() {
        const theme = localStorage.getItem('theme');
        if (theme === 'dark' || (!theme && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
          document.documentElement.classList.add('dark');
        }
      })();
    `;

    export default function RootLayout({
      children,
    }: Readonly<{
      children: React.ReactNode;
    }>) {
      return (
        <html
          lang="en"
          className={`${inter.variable} ${jetbrainsMono.variable} antialiased`}
          suppressHydrationWarning
        >
          <head>
            <script dangerouslySetInnerHTML={{ __html: themeScript }} />
          </head>
          <body className="min-h-full flex flex-col bg-background text-foreground">
            {children}
          </body>
        </html>
      );
    }
    ```

    Key changes from current:
    - Replace `Geist, Geist_Mono` imports with `Inter, JetBrains_Mono` (PER D-01)
    - Font CSS variables change from `--font-geist-sans`/`--font-geist-mono` to `--font-inter`/`--font-jetbrains-mono`
    - Add `suppressHydrationWarning` on `<html>` (prevents React mismatch from inline script)
    - Add inline blocking `<script>` in `<head>` for theme flash prevention (PER D-03)
    - Update `<body>` with `bg-background text-foreground` classes
    - Remove `h-full` from `<html>` className
    - Update metadata title/description from "Create Next App" defaults
  </action>
  <verify>
    <automated>
      grep -q 'Inter, JetBrains_Mono' src/app/layout.tsx &&
      grep -q '--font-inter' src/app/layout.tsx &&
      grep -q '--font-jetbrains-mono' src/app/layout.tsx &&
      grep -q 'suppressHydrationWarning' src/app/layout.tsx &&
      grep -q "localStorage.getItem('theme')" src/app/layout.tsx &&
      grep -q '"Tony Portfolio"' src/app/layout.tsx
    </automated>
  </verify>
  <done>
    Fonts switched from Geist to Inter + JetBrains Mono. Flash prevention script in place. Metadata updated. Body has background/text-foreground for correct theming.
  </done>
  <acceptance_criteria>
    - `grep 'Inter, JetBrains_Mono' src/app/layout.tsx` exits 0 (correct font imports)
    - `grep 'Geist' src/app/layout.tsx` exits 1 (Geist removed)
    - `grep 'suppressHydrationWarning' src/app/layout.tsx` exits 0
    - `grep -c 'localStorage' src/app/layout.tsx` equals 1 (theme persistence script)
    - `grep 'name=.*title' src/app/layout.tsx` exits 0 (metadata title preserved)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Clean up page.tsx boilerplate into minimal portfolio skeleton</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current content — stock create-next-app template)
  </read_first>
  <action>
    Replace the entire content of `src/app/page.tsx` with a minimal portfolio skeleton:

    ```typescript
    export default function Home() {
      return (
        <>
          {/* HERO — Phase 1.3 */}
          {/* ABOUT — Phase 1.4 */}
          {/* EXPERIENCE — Phase 1.4 */}
          {/* RESEARCH — Phase 1.5 */}
          {/* SKILLS — Phase 1.6 */}
          {/* CONTACT — Phase 1.6 */}
        </>
      );
    }
    ```

    Also remove these stock Next.js SVG files from `public/` (they are unused boilerplate):
    - `public/next.svg`
    - `public/vercel.svg`
    - `public/file.svg`
    - `public/globe.svg`
    - `public/window.svg`

    Run: `rm -f public/next.svg public/vercel.svg public/file.svg public/globe.svg public/window.svg`

    **Do NOT** import any components or data files yet — those will be wired in Phase 1.2+. This is purely a cleanup task.
  </action>
  <verify>
    <automated>
      ! grep -q 'get-started\|create.next.app\|next\.svg' src/app/page.tsx &&
      test ! -f public/next.svg &&
      test ! -f public/vercel.svg &&
      npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    Stock create-next-app boilerplate removed from page.tsx. SVG assets cleaned from public/. Build passes without errors.
  </done>
  <acceptance_criteria>
    - `grep -i 'get-started' src/app/page.tsx` exits 1 (no default create-next-app text)
    - `ls public/next.svg 2>&1` contains "No such file" (cleaned up)
    - `ls public/vercel.svg 2>&1` contains "No such file" (cleaned up)
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries exist in Phase 1.1. All content is static and authored by the developer. No user input, external API calls, or data persistence beyond localStorage for theme preference.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T (Tampering) | Inline theme script | accept | Inline script sets className on `<html>` using localStorage. No user-controllable input reaches `innerHTML`. The script content is authored by the developer and never modified by user data. |
| T-11-02 | S (Spoofing) | localStorage theme key | accept | Theme preference is non-sensitive. No authentication or session data involved. |
</threat_model>

<verification>
- [ ] `npm run build` succeeds (static export)
- [ ] `npm run dev` starts and page renders without console errors
- [ ] Tailwind utilities `bg-accent`, `text-accent`, `text-accent-secondary` are functional
- [ ] Dark mode is default when `prefers-color-scheme: dark` is active
- [ ] No flash of light background on dark-mode page load
- [ ] Browser DevTools shows Inter and JetBrains Mono as loaded fonts
- [ ] `grep -r 'font-geist' src/` returns no results
</verification>

<success_criteria>
| Criterion | How to Verify |
|-----------|---------------|
| Inter renders as body font | DevTools computed styles: `font-family` contains Inter |
| JetBrains Mono loads as code font | `font-mono-` utility resolves to JetBrains Mono |
| Accent color token available | `<div className="bg-accent">` renders cyan-teal color |
| No theme flash on dark mode | Open page with dark OS preference — no white flash |
| Boilerplate removed | page.tsx has no "get-started" or "next.svg" references |
| Build passes | `npm run build` exits 0 |
</success_criteria>

<output>
After completion, create `.planning/phases/11-project-scaffold-design-system/11-01-SUMMARY.md`
</output>
