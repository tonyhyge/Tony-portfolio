---
phase: 01.6-skills-contact
plan: 03
type: execute
wave: 2
depends_on: [01.6-01]
files_modified:
  - src/components/contact/ContactForm.tsx
  - src/components/contact/SocialLinks.tsx
  - src/components/contact/ContactSection.tsx
  - src/app/page.tsx
autonomous: false
requirements: [F7]
user_setup:
  - service: Formspree
    why: "Form backend — handles form submission, spam protection, email forwarding"
    env_vars:
      - name: NEXT_PUBLIC_FORMSPREE_ID
        source: "Formspree Dashboard -> Forms -> {form-name} -> Form Endpoint (the hash after /f/)"
        note: "For static export, this MUST be NEXT_PUBLIC_ prefix to bake in at build time. Alternatively, hardcode in useForm()."
    dashboard_config:
      - task: "Create a new form"
        location: "Formspree Dashboard -> Create Form"
        fields_to_add: "name (text), email (email), message (textarea)"
must_haves:
  truths:
    - "Contact form has Name, Email, Message fields"
    - "Form validates required fields and email format client-side"
    - "After successful submission, form is replaced by inline confirmation message"
    - "Error messages appear inline below each invalid field"
    - "Honeypot field is present but invisible to users"
    - "Social links row shows GitHub, LinkedIn, Google Scholar, Email icons"
    - "Contact section uses split layout: form left, social links right (desktop)"
    - "Section heading has Mail icon accent beside title"
    - "Contact section has staggered fade-in animation on scroll into viewport"
  artifacts:
    - path: "src/components/contact/ContactForm.tsx"
      provides: "Formspree-powered contact form with validation and confirmation"
      exports: ["ContactForm"]
    - path: "src/components/contact/SocialLinks.tsx"
      provides: "Social icon row with GitHub, LinkedIn, Google Scholar, Email"
      exports: ["SocialLinks"]
    - path: "src/components/contact/ContactSection.tsx"
      provides: "Split-layout contact section with form + social links"
      exports: ["ContactSection"]
  key_links:
    - from: "ContactSection"
      to: "ContactForm"
      via: "<ContactForm /> in left column"
    - from: "ContactSection"
      to: "SocialLinks"
      via: "<SocialLinks /> in right column"
    - from: "ContactForm"
      to: "Formspree API"
      via: "useForm(FORMSPREE_ID) hook submits to Formspree"
---

<objective>
Build the Contact section with Formspree contact form, social links row, split layout, visual heading differentiation, and scroll-triggered fade-in animation.

Purpose: Fulfill F7 (Contact Section) with Formspree backend (D-12), inline per-field validation (D-17), inline confirmation (D-16), social icon row (D-18/D-19), split layout (D-21), Mail icon-accented heading (D-22), and staggered fade-in animation (D-24).
Output: Three components in src/components/contact/, plus page.tsx integration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.6-skills-contact/01.6-CONTEXT.md
@.planning/phases/01.6-skills-contact/01.6-RESEARCH.md
@.planning/ROADMAP.md

## Plan 01 Details (dependency)
@.planning/phases/01.6-skills-contact/01.6-01-PLAN.md

## Source Files
@src/app/page.tsx
@src/components/section.tsx
@src/components/container.tsx
@src/components/ui/button.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ContactForm component with Formspree integration</name>
  <files>src/components/contact/ContactForm.tsx</files>
  <read_first>
    01.6-RESEARCH.md — Pattern 3 (Formspree useForm Integration), Pitfall 5 (Honeypot Accessibility)
    01.6-CONTEXT.md — D-12 through D-17 (Formspree, validation, confirmation, fields)
    src/components/ui/button.tsx — Button component for submit
    src/components/contact/ — directory does not exist, create it
  </read_first>
  <action>
    Create `src/components/contact/ContactForm.tsx` as a `"use client"` component.

    **Form ID:** Use `process.env.NEXT_PUBLIC_FORMSPREE_ID || "YOUR_FORM_ID"` as a fallback — the user must configure this. In the useForm call: `useForm(process.env.NEXT_PUBLIC_FORMSPREE_ID || "YOUR_FORM_ID")`. Add a comment in the source explaining where to get the form ID.

    **Imports:**
    - `import { useForm, ValidationError } from "@formspree/react"`
    - `import { Button } from "@/components/ui/button"`
    - `import { cn } from "@/lib/utils"`

    **State handling per useForm hook (RESEARCH.md Pattern 3):**
    - `const [state, handleSubmit] = useForm(formId)`

    **Successful submission (D-16):**
    - `if (state.succeeded)`: render a confirmation block replacing the form
    - Confirmation message: "Thanks, Minh will get back to you soon" in a styled container
    - Use a subtle icon (Send or Check from lucide-react) above the text
    - White/transparent background card, centered text, with `text-foreground` and `text-muted-foreground` sub-text

    **Form fields (D-13):**
    1. Name — `<input type="text" name="name" required>`, with `<label htmlFor="name">Name</label>`
    2. Email — `<input type="email" name="email" required>`, with `<label htmlFor="email">Email</label>`
    3. Message — `<textarea name="message" required rows={5} />`, with `<label htmlFor="message">Message</label>`

    **Field styling (Claude's discretion per D-22):**
    - Consistent with design system: border-border, rounded-lg, focus ring, padding
    - Labels: `text-sm font-medium` above each field
    - Input/textarea: `w-full px-3 py-2 border border-border rounded-lg bg-background text-foreground focus-visible:border-ring focus-visible:ring-[3px] focus-visible:ring-ring/50 outline-none transition-colors`

    **Inline validation errors (D-17):**
    - After each field, render `<ValidationError>` component from @formspree/react
    - The ValidationError shows errors inline for that specific field
    - When a field has an error, add a red border class to the input: `aria-invalid:border-destructive`

    **Honeypot (D-15):**
    - Per Pitfall 5 mitigation: do NOT use `display: none` or `hidden` attribute
    - Instead: `<input type="text" name="_gotcha" style={{ position: 'absolute', left: '-9999px', top: '-9999px' }} tabIndex={-1} autoComplete="off" aria-hidden="true" />`
    - This makes it invisible to both visual users AND screen readers

    **Submit button:**
    - `<Button type="submit" disabled={state.submitting}>Send Message</Button>` — or similar text
    - The button shows a disabled state while `state.submitting` is true
    - A small spinner or "Sending..." text while submitting (Claude's discretion)

    **Layout:** The form is vertical with `flex flex-col gap-5`. The form itself provides no wrapper — it's just the form element with tailwind classes.
  </action>
  <verify>
    <automated>grep -c "useForm" src/components/contact/ContactForm.tsx
      grep -c "UseForm" src/components/contact/ContactForm.tsx | head -1 || grep -c "formspree" src/components/contact/ContactForm.tsx
      grep -c "_gotcha" src/components/contact/ContactForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exports `ContactForm` as a named function component
    - Uses `useForm` hook from @formspree/react
    - Renders Name, Email, Message fields with labels
    - Shows inline ValidationError components below each field
    - On success (`state.succeeded`), shows confirmation message
    - Honeypot field uses `position: absolute; left: -9999px` pattern (not `display: none`)
    - Submit button is disabled while `state.submitting`
    - On submission error, errors appear inline
  </acceptance_criteria>
  <done>ContactForm created with Formspree useForm, inline validation, honeypot, and confirmation state</done>
</task>

<task type="auto">
  <name>Task 2: Create SocialLinks and ContactSection components</name>
  <files>src/components/contact/SocialLinks.tsx, src/components/contact/ContactSection.tsx</files>
  <read_first>
    01.6-CONTEXT.md — D-18 through D-24 (social links, split layout, visual headings, animations)
    src/components/section.tsx — Section wrapper
    src/components/contact/ContactForm.tsx — the form from Task 1 (just created)
    01.6-RESEARCH.md — Pattern 4 (Gradient Text) for section heading reference
  </read_first>
  <action>
    **Part A — Create `SocialLinks.tsx`:**

    `"use client"` component with no props.

    **Per D-18 (core 4 links) and D-19 (icon row, no labels):**
    - GitHub: `lucide-react` `Github` icon → `href="https://github.com/tonyhyge"`
    - LinkedIn: `Linkedin` icon → `href="https://linkedin.com/in/tonyhyge"` (use a sensible href — the user will update)
    - Google Scholar: `GraduationCap` icon (best match from lucide-react) → `href="https://scholar.google.com/citations?user=PLACEHOLDER"`
    - Email: `Mail` icon → `href="mailto:minhpq@example.com"` (the user will update)

    **Styling per D-24:**
    - Render 4 icons in a row with `flex items-center gap-4`
    - Each icon wrapped in an `<a>` tag with target="_blank" rel="noopener noreferrer"
    - Icon size: `className="h-6 w-6 text-muted-foreground hover:text-foreground transition-colors duration-200"`
    - Icons get a hover effect with subtle scale (Claude's discretion): `hover:scale-110`
    - Per D-20: no location text or availability status

    **Accessibility:**
    - Each link has `aria-label` with the platform name (e.g., `aria-label="GitHub"`)
    - `rel="noopener noreferrer"` on all external links

    **Part B — Create `ContactSection.tsx`:**

    `"use client"` component.

    **Section heading per D-22:**
    - Use `<Section>` wrapper but customize the heading: render heading area manually
    - The heading area shows the section number/title:
      - `h2` with text "Contact" + a Mail icon beside it (using lucide-react Mail)
      - The icon acts as the visual accent: `className="inline-block h-6 w-6 text-[var(--accent)] mr-2 align-middle"`
    - Subtitle: "Have a question or want to collaborate? Send me a message."
    - The heading styling should feel distinct from other sections as specified in D-22

    **Split layout per D-21:**
    - CSS grid: `grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12`
    - Left column: `<ContactForm />`
    - Right column: social links with a centered layout:
      - A div with `flex flex-col items-center justify-center h-full gap-6`
      - A small label "Find me on" or "Connect" in `text-muted-foreground text-sm`
      - `<SocialLinks />` below
    - On mobile: form on top, social links below (natural flex-col order)

    **Fade-in animation per D-24:**
    - Use IntersectionObserver: wrap form fields and social icons in staggered animation containers
    - On first intersection: set `isVisible` state
    - Apply transition classes: `transition-all duration-500 ease-out`
    - Stagger: form fields get delay increments of 100ms (0ms, 100ms, 200ms)
    - Social icons: individual icons get 50ms stagger starting at 300ms
    - Initial state: `opacity-0 translate-y-4` → visible: `opacity-100 translate-y-0`
    - Respect `prefers-reduced-motion`: if true, skip the animation (set visible immediately)

    **Edge cases:**
    - The section renders whether or not the Formspree form ID has been configured — it falls through to the "YOUR_FORM_ID" fallback which will display Formspree errors gracefully
    - All social links open in new tabs
  </action>
  <verify>
    <automated>grep -c "SocialLinks" src/components/contact/SocialLinks.tsx
      grep -c "ContactSection" src/components/contact/ContactSection.tsx
      grep -c "Github\|Linkedin\|GraduationCap\|Mail" src/components/contact/SocialLinks.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `SocialLinks.tsx` exports `SocialLinks` — renders 4 icon links with aria-labels
    - Icons: Github, Linkedin, GraduationCap (Google Scholar), Mail
    - No label text next to icons per D-19
    - `ContactSection.tsx` exports `ContactSection` — uses split grid layout
    - Heading includes Mail icon accent beside "Contact" text
    - Left column renders ContactForm, right column renders SocialLinks
    - IntersectionObserver triggers staggered fade-in
    - `prefers-reduced-motion` disables animation
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>SocialLinks and ContactSection created with split layout, icon accent heading, stagger fade-in</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Wire SkillsSection and ContactSection into page.tsx</name>
  <files>src/app/page.tsx</files>
  <read_first>
    src/app/page.tsx — current state with placeholder sections for Skills and Contact
    src/components/skills/SkillsSection.tsx — the skills section component from Plan 02
    src/components/contact/ContactSection.tsx — the contact section from Task 2
  </read_first>
  <action>
    Edit `src/app/page.tsx`:

    1. Add imports at the top: import `SkillsSection` from `@/components/skills/SkillsSection` and `ContactSection` from `@/components/contact/ContactSection`.

    2. Replace the skills placeholder `<section id="skills" className="min-h-screen" aria-label="Skills">` block (currently `{/* SKILLS — Phase 1.6 */}` inside) with `<SkillsSection />`.

    3. Replace the contact placeholder `<section id="contact" className="min-h-screen" aria-label="Contact">` block (currently `{/* CONTACT — Phase 1.6 */}` inside) with `<ContactSection />`.

    The SkillsSection and ContactSection components handle their own section wrappers, IDs, and aria-labels. No additional wrapping needed.

    Run `npm run build` to verify everything compiles and the static export succeeds.
  </action>
  <verify>
    <automated>grep -c "SkillsSection" src/app/page.tsx
      grep -c "ContactSection" src/app/page.tsx
      npm run build 2>&1 > /dev/null && echo "BUILD_PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` imports `SkillsSection` from `@/components/skills/SkillsSection`
    - `page.tsx` imports `ContactSection` from `@/components/contact/ContactSection`
    - Old placeholder section elements for skills and contact are removed
    - `npm run build` exits with code 0 (TypeScript + static export passes)
    - Skills section renders with gradient heading and accordion
    - Contact section renders with split layout, form, and social links
    - All sections in the page maintain correct order: Hero → About → Experience → Research → Skills → Contact
  </acceptance_criteria>
  <done>SkillsSection and ContactSection wired into page.tsx, build passes</done>
  <what-built>Final page.tsx integration of Skills and Contact sections</what-built>
  <how-to-verify>
    1. Run `npm run build` and confirm zero errors
    2. Run `npm run dev` and open the browser
    3. Scroll through the full page — sections appear in order: Hero, About, Experience, Research, Skills, Contact
    4. The Skills section has gradient heading text, accordion categories expand/collapse, skill popovers work
    5. The Contact section has form on left, social icons on right (desktop), or stacked (mobile)
    6. Fill out the contact form and submit — the form should show success confirmation (or Formspree error if form ID not configured yet)
    7. Social links icons render correctly and open in new tabs
    8. Mobile layout stacks sections vertically with correct order
    9. The nav anchors (#skills and #contact) scroll to the correct positions
    10. Prefers-reduced-motion disables the fade-in animation (test via OS accessibility settings or DevTools rendering tab)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<!-- Artifacts this phase produces -->
<!--
New:
- src/components/contact/ContactForm.tsx — ContactForm (Formspree useForm with inline validation + confirmation)
- src/components/contact/SocialLinks.tsx — SocialLinks (icon row: GitHub, LinkedIn, Google Scholar, Email)
- src/components/contact/ContactSection.tsx — ContactSection (split layout with stagger fade-in animation)

Modified:
- src/app/page.tsx — SkillsSection and ContactSection wired into the page
-->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contact form → Formspree API | User-submitted name, email, message sent to external service |
| Social links → external URLs | Navigation to GitHub, LinkedIn, Google Scholar, mailto: |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | Formspree form submission | mitigate | Formspree server-side sanitizes input; honeypot field blocks spam bots per D-15. Client-side uses HTML5 validation (`required`, `type=email`). |
| T-16-05 | Information Disclosure | Formspree form ID in client source | accept | Form ID is public by design (embedded in client HTML). No sensitive data — form only accepts name/email/message. |
| T-16-06 | Tampering | Social link URLs | mitigate | External links use `rel="noopener noreferrer"` to prevent tab-napping. All hrefs are static TypeScript strings — no user input. |
</threat_model>

<verification>
- All components type-check with `tsc --noEmit`
- `npm run build` completes successfully
- Form validation works: empty fields show errors, invalid email format shows error
- Form confirmation: submitting shows "Thanks, Minh will get back to you soon"
- Social icons: all 4 render with correct aria-labels and hrefs
- Split layout: CSS grid flips to stacked on mobile
- Stagger fade-in: IntersectionObserver triggers animation sequence
</verification>

<success_criteria>
- [ ] ContactForm, SocialLinks, ContactSection components created
- [ ] ContactForm uses @formspree/react useForm with inline validation
- [ ] Honeypot uses accessibility-safe positioning (not display:none)
- [ ] SocialLinks renders 4 icon-only links with aria-labels
- [ ] ContactSection uses split grid layout (2-col desktop, 1-col mobile)
- [ ] Heading has Mail icon accent
- [ ] Staggered fade-in animation with prefers-reduced-motion support
- [ ] SkillsSection and ContactSection wired into page.tsx
- [ ] Build passes with zero errors
- [ ] Human verified: form renders, validation works, confirmation shows, social links work
</success_criteria>

<output>
Create `.planning/phases/01.6-skills-contact/01.6-03-SUMMARY.md` when done
</output>
