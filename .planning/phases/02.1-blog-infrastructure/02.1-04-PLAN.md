---
phase: 02.1-blog-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["02.1-01", "02.1-02"]
files_modified: []
files_created:
  - src/app/data/blog.test.ts
  - src/lib/blog.test.ts
autonomous: true
requirements: [F6]
must_haves:
  truths:
    - "estimateReadingTime returns 0 for empty content"
    - "estimateReadingTime returns 1 for ~200 words"
    - "estimateReadingTime rounds up fractional minutes"
    - "formatPostDate formats ISO date to readable form"
    - "BlogPostSchema.parse validates correct frontmatter"
    - "BlogPostSchema.parse throws on missing title"
    - "BlogPostSchema.parse throws on invalid date"
    - "BlogPostSchema.parse applies default values (draft=false, tags=[])"
  artifacts:
    - path: "src/app/data/blog.test.ts"
      provides: "Unit tests for BlogPostSchema validation"
      min_lines: 40
      contains: "describe(\"BlogPostSchema\""
    - path: "src/lib/blog.test.ts"
      provides: "Unit tests for estimateReadingTime and formatPostDate"
      min_lines: 30
      contains: "describe(\"estimateReadingTime\""
  key_links: []
---

<objective>
Write unit tests for the blog data schema and utility functions.

Purpose: Ensure 80%+ coverage for src/app/data/blog.ts, src/lib/blog.ts before Phase 2.2 builds on top. Validate that the Zod schema correctly validates/invalidates frontmatter, and that reading time and date formatting utilities behave correctly at edge cases.

Output: src/lib/blog.test.ts (utility tests), src/app/data/blog.test.ts (schema+loader tests)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/minhpham/Claude/Projects/personal portfolio/src/lib/utils.test.ts
@/Users/minhpham/Claude/Projects/personal portfolio/src/app/data/research.ts
@/Users/minhpham/Claude/Projects/personal portfolio/vitest.config.ts
@/Users/minhpham/Claude/Projects/personal portfolio/.planning/phases/02.1-blog-infrastructure/02.1-CONTEXT.md
@/Users/minhpham/Claude/Projects/personal portfolio/.planning/phases/02.1-blog-infrastructure/02.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write utility function tests for estimateReadingTime and formatPostDate</name>
  <files>src/lib/blog.test.ts</files>
  <action>
    Create `src/lib/blog.test.ts` following the analog pattern from `src/lib/utils.test.ts`.

    Use vitest (`describe`, `it`, `expect` from "vitest"). Import `estimateReadingTime` and `formatPostDate` from `@/lib/blog`.

    **Test cases for `estimateReadingTime()`** (per D-09):
    - Empty string returns 0
    - Single word returns 1
    - Exactly 200 words returns 1
    - 201 words returns 2 (ceiling rounding)
    - 400 words returns 2
    - Content with only whitespace returns 0
    - Content with mixed whitespace (tabs, newlines) counts correctly

    **Test cases for `formatPostDate()`**:
    - "2026-06-22" returns "June 22, 2026"
    - "2024-01-05" returns "January 5, 2024"
    - Invalid input like empty string returns the input as-is (graceful fallback)
  </action>
  <verify>
    <automated>cd "/Users/minhpham/Claude/Projects/personal portfolio" && npx vitest run src/lib/blog.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All estimateReadingTime and formatPostDate tests pass with vitest.</done>
</task>

<task type="auto">
  <name>Task 2: Write blog data model tests for BlogPostSchema and loader functions</name>
  <files>src/app/data/blog.test.ts</files>
  <action>
    Create `src/app/data/blog.test.ts`.

    Import `BlogPostSchema` from `@/app/data/blog` and vitest functions.

    **Test cases for `BlogPostSchema.parse()`** (per D-02, D-04):
    - Valid minimal frontmatter (title + date only) passes, tags defaults to [], draft defaults to false
    - Valid complete frontmatter (all 9 fields) passes
    - Missing title throws ZodError
    - Empty title string throws ZodError
    - Invalid date format ("2026/06/22") throws ZodError
    - Invalid date format ("June 22, 2026") throws ZodError
    - Invalid canonical URL (not a URL string) throws ZodError
    - draft: true is accepted
    - `updated` with valid ISO date passes
    - `series` with string value preserves the value

    **Loader function tests:**
    These tests validate the schema parsing logic and type inference. The fs-based loaders (getAllSlugs, getPostBySlug, getAllPosts) require test fixtures and may be skipped if vitest cannot resolve fs in jsdom environment. If fs is unavailable in tests:

    1. Test `BlogPostSchema.parse()` thoroughly (no fs dependency)
    2. Add a placeholder test that the module exports: `expect(getAllPosts).toBeDefined()`
    3. Note in the test file that full loader tests require integration testing (build-level)

    Use `@/__fixtures__/index.ts` pattern if needed for shared test constants.
  </action>
  <verify>
    <automated>cd "/Users/minhpham/Claude/Projects/personal portfolio" && npx vitest run src/app/data/blog.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All BlogPostSchema parse tests pass; module export test confirms loader functions are defined.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner | vitest executes test files in jsdom environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-06 | Tampering | test execution | accept | Tests run in CI pipeline; no production impact |
</threat_model>

<verification>
- `npm test` passes (all tests including existing ones)
- Coverage for `src/lib/blog.ts` >= 80%
- Coverage for `src/app/data/blog.ts` schema parsing >= 80%
</verification>

<success_criteria>
- `src/lib/blog.test.ts` - all estimateReadingTime and formatPostDate tests pass
- `src/app/data/blog.test.ts` - all BlogPostSchema parse tests pass
- `npm test` exits 0
</success_criteria>

<output>
Create `.planning/phases/02.1-blog-infrastructure/02.1-04-SUMMARY.md` when done
</output>
