---
phase: 02.1-blog-infrastructure
plan: 01
subsystem: blog-engine
tags: ["mdx", "infrastructure", "dependencies"]
requires: []
provides: ["next-mdx-remote", "gray-matter", "rehype-pretty-code", "hello-world fixture"]
affects: ["package.json", "package-lock.json", "content/blog/"]
tech-stack:
  added: ["next-mdx-remote@6.0.0", "gray-matter@4.0.3", "rehype-pretty-code@0.14.3"]
  patterns: []
key-files:
  created: ["content/blog/hello-world.mdx"]
  modified: ["package.json", "package-lock.json"]
  deleted: ["content/blog/.gitkeep"]
decisions:
  - "@types/gray-matter not installed — gray-matter ships its own bundled types"
metrics:
  duration: "4m"
  completed: "2026-06-22T10:40:00Z"
status: complete
---

# Phase 2.1 Plan 01: Install Blog Dependencies & Fixture Summary

Install core MDX blog engine packages and create a sample blog post fixture for build-time validation.

## Tasks

| # | Name | Commit | Key Files |
|---|------|--------|-----------|
| 1 | Install npm packages for MDX blog engine | d86f093 | package.json, package-lock.json |
| 2 | Create sample MDX blog post fixture | bb7fbf6 | content/blog/hello-world.mdx |

## Task Details

### Task 1: Install npm packages for MDX blog engine

Ran `npm install next-mdx-remote gray-matter rehype-pretty-code`. All three packages installed successfully:

- `next-mdx-remote@6.0.0` — Build-time MDX serialization with `<MDXRemote>` rendering
- `gray-matter@4.0.3` — Frontmatter YAML parsing
- `rehype-pretty-code@0.14.3` — Build-time syntax highlighting via shiki

`@types/gray-matter` was skipped — the `gray-matter` package ships its own bundled TypeScript definitions (`gray-matter.d.ts`), making the separate DefinitelyTyped package redundant. This was verified by inspecting `node_modules/gray-matter/package.json` which confirms `types: gray-matter.d.ts`.

Verification: `node -e "require('next-mdx-remote'); require('gray-matter'); require('rehype-pretty-code')"` returns OK. `npm ls` shows all three packages in the dependency tree.

### Task 2: Create sample MDX blog post fixture

Created `content/blog/hello-world.mdx` with all 9 frontmatter fields populated according to D-02 schema:

- title, date, tags, excerpt, cover, draft, updated, series, canonical

Body content (324 words) includes:
- Level-2 heading (`## Welcome to the Blog`)
- **Bold** and _italic_ text
- TypeScript code block with language tag (```typescript)
- Inline `code` snippet

Deleted `content/blog/.gitkeep` after creation.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Plan inaccuracy] `@types/gray-matter` package not found in registry**
- **Triggered by:** Plan Task 1 called `npm install --save-dev @types/gray-matter`
- **Found during:** Task 1 install attempt
- **Issue:** `npm install @types/gray-matter` returned 404 — the package does not exist on npm
- **Fix:** Verified that `gray-matter` ships its own bundled TypeScript types. Skipped the dev dependency entirely. This is a common pattern for well-maintained npm packages — no external `@types/` wrapper needed.
- **Files modified:** None (only the devDependency was omitted from package.json)

## Threat Surface Scan

No new threat surface introduced. npm registry packages are lockfile-pinned per T-02.1-01. No endpoints, auth paths, or file-access patterns were added.

## Verification

- `npm ls next-mdx-remote gray-matter rehype-pretty-code` — all three at expected versions
- `node -e "require('next-mdx-remote'); require('gray-matter'); require('rehype-pretty-code')"` — all resolve without error
- `content/blog/hello-world.mdx` contains all 9 frontmatter fields and 324 words of body content
- `.gitkeep` removed, directory clean

## Self-Check: PASSED

All tasks verified, commits present, no untracked files.
