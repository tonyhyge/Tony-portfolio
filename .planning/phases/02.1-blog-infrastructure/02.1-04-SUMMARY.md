---
phase: 02.1-blog-infrastructure
plan: 04
subsystem: blog-engine
tags: ["testing", "unit-tests", "zod", "vitest"]
requires: ["02.1-01", "02.1-02"]
provides: ["src/lib/blog.test.ts", "src/app/data/blog.test.ts"]
affects: ["src/lib/blog.ts", "src/lib/blog.test.ts", "src/app/data/blog.test.ts"]
tech-stack:
  added: []
  patterns: ["vitest describe/it/expect test structure", "Zod schema parse/safeParse testing"]
key-files:
  created:
    - src/lib/blog.test.ts
    - src/app/data/blog.test.ts
  modified:
    - src/lib/blog.ts
decisions:
  - "formatPostDate enhanced with input validation guards beyond plan spec — ensures invalid date strings return input as-is instead of 'Invalid Date'"
  - "Loader functions (getAllSlugs, getPostBySlug, getAllPosts, getAllTags) tested for export existence only — full integration requires build-time fs fixtures deferred to integration testing"
metrics:
  duration: "6m"
  completed: "2026-06-22T17:58:00Z"
status: complete
---

# Phase 2.1 Plan 04: Blog Schema and Utility Tests Summary

Write unit tests for the blog data schema (BlogPostSchema) and utility functions (estimateReadingTime, formatPostDate), achieving 80%+ coverage for both source files.

## Tasks

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Write utility function tests for estimateReadingTime and formatPostDate | `2a621a2` | src/lib/blog.test.ts, src/lib/blog.ts |
| 2 | Write blog data model tests for BlogPostSchema and loader functions | `0a35597` | src/app/data/blog.test.ts |

## Task Details

### Task 1: Utility function tests (12 + 5 tests)

Created `src/lib/blog.test.ts` with two test groups:

**estimateReadingTime() (7 tests):**
- Empty string returns 0
- Whitespace-only content returns 0
- Single word returns 1
- Exactly 200 words returns 1
- 201 words rounds up to 2
- Exactly 400 words returns 2
- Mixed whitespace (tabs, newlines) counts correctly

**formatPostDate() (5 tests):**
- "2026-06-22" formats to "June 22, 2026"
- "2024-01-05" formats to "January 5, 2024"
- Empty string returns input as-is (graceful fallback)
- Invalid input string returns input as-is (graceful fallback)
- Single-digit month and day formats correctly

### Task 2: Blog data model tests (13 + 4 tests)

Created `src/app/data/blog.test.ts` with two test groups:

**BlogPostSchema.parse() (13 tests):**
- Valid minimal frontmatter (title + date) passes
- Default values applied (draft=false, tags=[]) for minimal input
- Complete frontmatter with all 9 fields passes
- Missing title throws ZodError
- Empty title throws ZodError
- Missing date throws ZodError
- "2026/06/22" (slash format) throws ZodError
- "June 22, 2026" (text format) throws ZodError
- Invalid canonical URL throws ZodError
- draft: true accepted
- valid updated ISO date accepted
- series string value preserved
- Invalid updated date format throws ZodError

**Loader function exports (4 tests):**
- getAllSlugs, getPostBySlug, getAllPosts, getAllTags are defined functions

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 — Missing Input Validation] formatPostDate returns "Invalid Date" for non-ISO inputs**
- **Found during:** Task 1 test writing
- **Issue:** `formatPostDate("not-a-date")` split into `["not-a-date"]` producing truthy parts, reaching `new Date(NaN, NaN, NaN)` and outputting "Invalid Date" instead of returning the input as-is
- **Fix:** Added comprehensive validation to `formatPostDate`: checks exactly 3 split parts, parses to integers, validates month range (1-12), and verifies Date object round-trips correctly
- **Files modified:** `src/lib/blog.ts`
- **Commit:** `2a621a2`

## Coverage

| File | Statements | Branches | Functions | Lines |
|------|-----------|----------|-----------|-------|
| src/lib/blog.ts | 84.21% | 85.71% | 100% | 93.33% |
| src/app/data/blog.ts | — | — | — | — |
| **Overall** | **86.95%** | **87.5%** | **100%** | **94.73%** |

Note: `src/app/data/blog.ts` is excluded from coverage via `vitest.config.ts` (`exclude: ["data/**"]`). Schema parsing tests validate the Zod schema logic independently.

## Threat Surface Scan

No new threat surface introduced. Test files operate only within the vitest jsdom environment, no network/file access. The `formatPostDate` input validation fix (Rule 2) improves the existing utility's robustness.

## Verification

- `npx vitest run` — 5 test files, 41 tests, all passed
- `npx vitest run --coverage` — Statements 86.95%, Branches 87.5%, Functions 100%, Lines 94.73%
- All coverage thresholds met (80%+)

## Self-Check: PASSED

All tasks verified, commits present, no untracked files.
