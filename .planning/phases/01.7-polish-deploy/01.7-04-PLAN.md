---
phase: 01.7-polish-deploy
plan: 04
type: execute
wave: 2
depends_on: [01.7-01]
files_modified:
  - tests/e2e/portfolio.spec.ts
  - tests/e2e/responsive.spec.ts
  - tests/e2e/a11y.spec.ts
autonomous: false
requirements: [NFR1, NFR2, NFR3]
user_setup: []

must_haves:
  truths:
    - "E2E tests verify hero section loads with heading and CTA"
    - "E2E tests verify section navigation via nav links"
    - "E2E tests verify dark mode toggle exists and toggles .dark class"
    - "E2E tests verify contact form fields are present"
    - "E2E tests capture full-page screenshots at 320px, 768px, 1024px, 1440px"
    - "axe-core a11y tests assert no WCAG AA violations on home page in light and dark mode"
    - "axe-core a11y tests assert no WCAG AA violations on 404 page"
    - "Skip-to-content link exists in layout and is keyboard-focusable (D-14)"
    - "Radix Sheet focus trap verified: mobile nav keeps focus within sheet when open (D-14)"
  artifacts:
    - path: "tests/e2e/portfolio.spec.ts"
      provides: "E2E tests for nav, section loading, dark mode, form fields"
      min_lines: 40
    - path: "tests/e2e/responsive.spec.ts"
      provides: "Screenshot capture at 320/768/1024/1440 breakpoints"
      min_lines: 20
    - path: "tests/e2e/a11y.spec.ts"
      provides: "WCAG AA audit via axe-core for home page (light + dark) and 404"
      min_lines: 30
  key_links:
    - from: "tests/e2e/portfolio.spec.ts"
      to: "playwright.config.ts"
      via: "baseURL from config, uses /Tony-portfolio/ route"
      pattern: "Tony-portfolio"
    - from: "tests/e2e/a11y.spec.ts"
      to: "@axe-core/playwright"
      via: "AxeBuilder import"
      pattern: "@axe-core/playwright"
    - from: "tests/e2e/responsive.spec.ts"
      to: "playwright.config.ts"
      via: "viewport size setting and screenshot capture"
      pattern: "setViewportSize"
---
<objective>
Write Playwright E2E tests for portfolio navigation/sections/theme, responsive screenshot capture at 4 breakpoints, and WCAG AA accessibility audit via axe-core with aplaywright injection.

Purpose: Cover the user-facing verification layer of the testing pyramid — E2E tests for critical paths per D-02, responsive screenshots per D-02 (320/768/1024/1440), and automated WCAG AA audits per D-13 (axe-core integrated into Playwright).
Output: 3 E2E test files in tests/e2e/, ending with a human-verify checkpoint for first run results.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.7-polish-deploy/01.7-RESEARCH.md
@.planning/phases/01.7-polish-deploy/01.7-CONTEXT.md
@src/app/layout.tsx
@src/components/theme-toggle.tsx
@playwright.config.ts
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E portfolio navigation and interaction tests</name>
  <files>tests/e2e/portfolio.spec.ts</files>
  <action>
    Create `tests/e2e/portfolio.spec.ts` per RESEARCH.md Pattern E2E test code. Import `{ test, expect }` from `@playwright/test`.

    Test structure (describes):
    - `test.describe("Portfolio homepage", ...)`

    Test cases with `test.beforeEach` navigating to `/Tony-portfolio/`:
    1. `"loads hero section with heading"`: h1 visible, text "View Projects" visible
    2. `"navigates sections via nav links"`: count nav links > 0, click each nav link and verify no error
    3. `"toggles dark mode"`: find theme toggle button via aria-label="Toggle theme" (confirmed existing in theme-toggle.tsx), click, expect html to have class "dark", click again, expect not dark
    4. `"contact section has form fields"`: navigate to `/#contact`, expect `input[name="name"]`, `input[name="email"]`, `textarea[name="message"]` visible
    5. `"loads 404 page correctly"`: navigate to `/Tony-portfolio/nonexistent`, expect "Lost in the Lab?" text visible, expect "Back to Home" link visible, click it and verify URL returns to `/Tony-portfolio/`

    Use accessible queries (getByRole, getByText, getByLabelText) per RESEARCH.md anti-patterns — avoid getByTestId unless necessary.
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/e2e/portfolio.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 5 E2E test cases pass: hero loads, nav links work, dark mode toggles, contact form renders, 404 page renders correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create responsive screenshot and a11y audit tests</name>
  <files>tests/e2e/responsive.spec.ts, tests/e2e/a11y.spec.ts</files>
  <action>
    Create two E2E test files.

    **tests/e2e/responsive.spec.ts** per RESEARCH.md Pattern test code:
    - Import `{ test }` from `@playwright/test`
    - Define `BREAKPOINTS` array: `[{ width: 320, name: "mobile-small" }, { width: 768, name: "tablet" }, { width: 1024, name: "desktop" }, { width: 1440, name: "desktop-wide" }]`
    - `test.describe("Responsive screenshot tests")`
    - Loop over breakpoints, for each:
      - Set viewport size: `{ width, height: 900 }`
      - Navigate to `/Tony-portfolio/`
      - `waitForLoadState("networkidle")`
      - Full-page screenshot: `page.screenshot({ path: `screenshots/${name}.png`, fullPage: true })`
    - Create `screenshots/` dir at project root if it doesn't exist (screenshots are .gitignore'd)

    **tests/e2e/a11y.spec.ts** per RESEARCH.md Pattern 4:
    - Import `{ test, expect }` from `@playwright/test`, `AxeBuilder` from `@axe-core/playwright`
    - `test.describe("Accessibility (WCAG AA)", ...)`
    - Test cases:
      1. `"home page has no WCAG AA violations"`: navigate to `/Tony-portfolio/`, run `AxeBuilder({ page }).withTags(["wcag2a", "wcag2aa"]).analyze()`, assert `results.violations` is empty
      2. `"dark mode has no WCAG AA violations"`: navigate to `/Tony-portfolio/`, click theme toggle (aria-label="Toggle theme"), wait for `html.dark`, run axe-core analysis, assert violations empty
      3. `"404 page has no WCAG AA violations"`: navigate to `/Tony-portfolio/nonexistent`, run axe-core analysis, assert violations empty
    - No manual `inject()` call needed — @axe-core/playwright injects automatically per RESEARCH.md Pattern 4 and Pitfall 4.

    For both test files, use `describe` and basic `test` blocks.
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/e2e/a11y.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>
    Responsive screenshots captured at all 4 breakpoints. axe-core audits pass for home page (light + dark) and 404 page with zero WCAG AA violations.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Playwright E2E tests:
    - tests/e2e/portfolio.spec.ts (5 test cases)
    - tests/e2e/responsive.spec.ts (4 screenshot test cases)
    - tests/e2e/a11y.spec.ts (3 WCAG AA audit test cases)
  </what-built>
  <how-to-verify>
    1. Run `npm run build` to produce the static export
    2. Run `npx playwright test --reporter=list` to execute the full E2E suite
    3. Verify all 12+ test cases pass (no failures)
    4. Check that axe-core a11y audits report zero WCAG AA violations
    5. Check that responsive screenshots were generated (check screenshots/ directory exists)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any failures to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test script -> live site | Playwright navigates against served static export |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | axe-core analysis | accept | axe-core runs in isolated Playwright context against static HTML |
</threat_model>

<verification>
- `npm run build` succeeds (required before E2E: static export must exist for serve)
- `npx playwright test --reporter=list` — all tests green
- axe-core reports 0 violations for WCAG AA on all tested pages
</verification>

<success_criteria>
12+ E2E test cases all passing. 0 WCAG AA violations. Screenshots captured at 4 breakpoints.
</success_criteria>

<output>
Create `.planning/phases/01.7-polish-deploy/01.7-04-SUMMARY.md` when done
</output>
