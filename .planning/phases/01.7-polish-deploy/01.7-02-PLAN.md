---
phase: 01.7-polish-deploy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/not-found.tsx
  - src/app/robots.ts
  - src/app/sitemap.ts
autonomous: true
requirements: [NFR4, NFR6]
user_setup: []

must_haves:
  truths:
    - "Visiting an unknown route shows a creative hexagon-themed 404 page"
    - "404 page has a 'Back to Home' link navigating to /Tony-portfolio/"
    - "robots.txt allows all AI crawlers (GPTBot, ClaudeBot, PerplexityBot, OAI-SearchBot)"
    - "robots.txt links to sitemap.xml with production URL"
    - "sitemap.xml includes home page + research project entries with production URLs"
    - "Navigation to a valid page works without 404 interference"
  artifacts:
    - path: "src/app/not-found.tsx"
      provides: "Custom 404 page with hexagon motif, playful copy, and back-to-home link"
      min_lines: 25
    - path: "src/app/robots.ts"
      provides: "Dynamic robots.txt allowing AI crawlers with sitemap reference"
      min_lines: 15
    - path: "src/app/sitemap.ts"
      provides: "Dynamic sitemap covering home + research project pages"
      min_lines: 20
  key_links:
    - from: "src/app/not-found.tsx"
      to: "next/link"
      via: "Link component for 'Back to Home' navigation"
      pattern: "from.*next/link"
    - from: "src/app/sitemap.ts"
      to: "src/app/data/research.ts"
      via: "imports researchProjects for slug enumeration"
      pattern: "researchProjects"
---
<objective>
Create the custom 404 page (hexagon-themed, playful "Lost in the Lab?" design per UI-SPEC.md), dynamic robots.txt (AI-crawler-friendly per D-21), and dynamic sitemap.xml (home + research projects per D-22).

Purpose: Cover go-live artifacts that make the site production-ready — a polished error experience for users and proper SEO signals for crawlers.
Output: src/app/not-found.tsx, src/app/robots.ts, src/app/sitemap.ts
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.7-polish-deploy/01.7-UI-SPEC.md
@.planning/phases/01.7-polish-deploy/01.7-RESEARCH.md
@.planning/phases/01.7-polish-deploy/01.7-CONTEXT.md
@src/app/layout.tsx
@src/app/data/research.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hexagon-themed custom 404 page</name>
  <files>src/app/not-found.tsx</files>
  <action>
    Create `src/app/not-found.tsx` implementing the UI-SPEC.md design contract for the 404 page.

    Structure and design requirements:
    - Default export function `NotFound()` — Next.js convention for 404 page
    - Full viewport centering: `flex min-h-screen flex-col items-center justify-center gap-6 px-4 text-center`
    - Page title set via metadata export: `export const metadata = { title: "404 — Page Not Found | Tony Pham" }`
    - Hexagon shape with "404" inside using CSS clip-path clipping a div (decorative, aria-hidden). Same clip-path as Hero's HexagonBadge: `polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%)`. Background uses `--gradient-hero` CSS variable (existing token). Inside: `<span className="text-3xl font-bold text-primary-foreground">404</span>`
    - Heading: `<h1 className="text-2xl font-semibold text-foreground">Lost in the Lab?</h1>`
    - Body: `<p className="max-w-md text-muted-foreground">The page you are looking for does not exist. It may have been moved, or the link you followed might be broken.</p>`
    - CTA: `<Link href="/Tony-portfolio/" className="inline-flex items-center justify-center rounded-lg bg-accent px-6 py-3 text-sm font-medium text-accent-foreground transition-colors hover:bg-accent/90 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring/50">Back to Home</Link>`
    - Import `Link` from `next/link`
    - Hexagon container uses `aria-hidden="true"` per UI-SPEC a11y notes (decorative only).
    - Use `<html lang="en">` inheritance from root layout — no need to set again.
    - No new CSS, no new dependencies — all styling uses shadcn utility classes and existing CSS variables per D-20.
    - Responds to `.dark` class toggle via existing CSS variable swapping in globals.css --gradient-hero stays same across themes per UI-SPEC color contract.
  </action>
  <verify>
    <automated>test -f src/app/not-found.tsx && grep -q "Lost in the Lab" src/app/not-found.tsx && grep -q "Back to Home" src/app/not-found.tsx && grep -q "clipPath" src/app/not-found.tsx && echo "pass" || echo "fail"</automated>
  </verify>
  <done>
    Custom 404 page renders hexagon with 404, "Lost in the Lab?" heading, descriptive body text, and accent-colored "Back to Home" link pointing to /Tony-portfolio/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AI-crawler-friendly robots.txt route handler</name>
  <files>src/app/robots.ts</files>
  <action>
    Create `src/app/robots.ts` implementing RESEARCH.md Pattern 6 per D-21:

    - Import `type MetadataRoute` from `next`
    - Default export function `robots(): MetadataRoute.Robots`
    - Rules:
      1. `{ userAgent: "*", allow: "/" }` — base allowance for all standard crawlers
      2. `{ userAgent: "GPTBot", allow: "/" }` — explicit for AI training crawler
      3. `{ userAgent: "ClaudeBot", allow: "/" }` — explicit for Anthropic crawler
      4. `{ userAgent: "PerplexityBot", allow: "/" }` — explicit for Perplexity crawler
      5. `{ userAgent: "OAI-SearchBot", allow: "/" }` — explicit for OpenAI search crawler
    - `sitemap: "https://tonyhyge.github.io/Tony-portfolio/sitemap.xml"` — production URL per D-21 note

    Per RESEARCH.md Pitfall note on robots.ts: The sitemap URL must use the production domain, not localhost. The `*` rule already allows all crawlers; explicit AI crawler entries serve as documentation per D-21.
  </action>
  <verify>
    <automated>test -f src/app/robots.ts && grep -q "GPTBot" src/app/robots.ts && grep -q "ClaudeBot" src/app/robots.ts && grep -q "OAI-SearchBot" src/app/robots.ts && grep -q "sitemap" src/app/robots.ts && echo "pass" || echo "fail"</automated>
  </verify>
  <done>
    robots.txt generated at build time allows all crawlers including explicit GPTBot, ClaudeBot, PerplexityBot, OAI-SearchBot entries with sitemap reference to production URL.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create dynamic sitemap route handler from research data</name>
  <files>src/app/sitemap.ts</files>
  <action>
    Create `src/app/sitemap.ts` implementing RESEARCH.md Pattern 5 per D-22:

    - Import `type MetadataRoute` from `next`
    - Import `researchProjects` from `@/app/data/research`
    - `const BASE_URL = "https://tonyhyge.github.io/Tony-portfolio"`
    - Default export `sitemap(): MetadataRoute.Sitemap`
    - Static entries:
      - `{ url: BASE_URL, lastModified: new Date(), changeFrequency: "monthly", priority: 1 }`
    - Dynamic entries from researchProjects:
      - Map each project to `{ url: `${BASE_URL}/#research`  (research projects are on the single page via anchor), lastModified: new Date(), changeFrequency: "monthly", priority: 0.8 }`
    - Return combined array

    Note: Research projects are part of the single-page portfolio, so sitemap points to `/#research` section anchor per RESEARCH.md Pitfall note. If research detail pages are added as separate routes later, this can be updated.
  </action>
  <verify>
    <automated>test -f src/app/sitemap.ts && grep -q "researchProjects" src/app/sitemap.ts && grep -q "tonyhyge" src/app/sitemap.ts && grep -q "MetadataRoute" src/app/sitemap.ts && echo "pass" || echo "fail"</automated>
  </verify>
  <done>
    sitemap.xml generated at build time includes home page entry at priority 1 and all research projects at priority 0.8.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public -> sitemap/robots | Publicly accessible metadata files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02 | Information Disclosure | sitemap.xml | accept | Public portfolio site; listing all URLs is intentional per SEO best practice |
</threat_model>

<verification>
- `npm run build` succeeds (generates 404.html in ./out, verifies robots.ts and sitemap.ts compile)
- Static export includes `./out/404.html` (Next.js not-found.tsx convention for static export)
</verification>

<success_criteria>
Custom 404 page renders in build output. robots.ts and sitemap.ts generate correctly at build time.
</success_criteria>

<output>
Create `.planning/phases/01.7-polish-deploy/01.7-02-SUMMARY.md` when done
</output>
