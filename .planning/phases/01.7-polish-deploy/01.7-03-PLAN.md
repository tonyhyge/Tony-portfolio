---
phase: 01.7-polish-deploy
plan: 03
type: execute
wave: 2
depends_on: [01.7-01]
files_modified:
  - src/lib/utils.test.ts
  - src/components/ui/button.test.tsx
  - src/components/ui/badge.test.tsx
  - src/__fixtures__/index.ts
autonomous: true
requirements: [NFR1, NFR2]
user_setup: []

must_haves:
  truths:
    - "cn() utility is unit-tested for class merging, tailwind-merge resolution, conditional classes, and empty input"
    - "Button component is tested for rendering, onClick, disabled state, and variant classes"
    - "Badge component is tested for rendering and variant application"
    - "Shared test fixtures exist for reuse across tests"
    - "Coverage threshold of 80% line + branch is achievable for src/lib/ code"
  artifacts:
    - path: "src/lib/utils.test.ts"
      provides: "Unit tests for cn() — merge, tailwind-merge resolution, conditionals, empty"
      min_lines: 20
    - path: "src/components/ui/button.test.tsx"
      provides: "Component tests for Button — render, click, disabled, variants"
      min_lines: 30
    - path: "src/components/ui/badge.test.tsx"
      provides: "Component tests for Badge — render, variant classes"
      min_lines: 20
    - path: "src/__fixtures__/index.ts"
      provides: "Shared test fixture for fixture patterns (D-06)"
      min_lines: 5
  key_links:
    - from: "src/lib/utils.test.ts"
      to: "src/lib/utils.ts"
      via: "imports cn() from source"
      pattern: "from.*@/lib/utils"
    - from: "src/components/ui/button.test.tsx"
      to: "src/components/ui/button.tsx"
      via: "imports Button component"
      pattern: "from.*@/components/ui/button"
    - from: "src/components/ui/badge.test.tsx"
      to: "src/components/ui/badge.tsx"
      via: "imports Badge component"
      pattern: "from.*@/components/ui/badge"
---
<objective>
Write unit tests for the cn() utility and component tests for Button (including variant coverage) and Badge (variant coverage). Create shared fixture data at src/__fixtures__/index.ts. All tests colocated per D-05 convention.

Purpose: Deliver the first layer of the testing pyramid — unit and component tests targeting the 80% coverage threshold for utility code (D-04). These are the highest-leverage test targets: cn() is used everywhere, Button and Badge are the most commonly reused UI components.
Output: 3 test files + 1 fixture file, all passing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.7-polish-deploy/01.7-RESEARCH.md
@.planning/phases/01.7-polish-deploy/01.7-CONTEXT.md
@.planning/codebase/TESTING.md
@src/lib/utils.ts
@src/components/ui/button.tsx
@src/components/ui/badge.tsx
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared test fixtures file</name>
  <files>src/__fixtures__/index.ts</files>
  <action>
    Create `src/__fixtures__/index.ts` per D-06. Export sample data objects that can be reused across tests. Include:
    - A sample valid className pair for cn() tests
    - A placeholder for component props (e.g., sample button children text)
    - Export as named constants with `as const` for type safety

    This file serves as the fixture pattern foundation. Keep it minimal — it establishes the pattern per D-06. Actual component-specific fixtures go colocated with the components.
  </action>
  <verify>
    <automated>test -f src/__fixtures__/index.ts && echo "pass" || echo "fail"</automated>
  </verify>
  <done>
    src/__fixtures__/index.ts exists with exported fixture constants.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for cn() utility</name>
  <files>src/lib/utils.test.ts</files>
  <action>
    Create `src/lib/utils.test.ts` colocated with utils.ts per D-05 convention. Test the `cn()` function using RESEARCH.md Pattern test code as reference:

    - `describe("cn()", ...)` suite with vitest (import { describe, it, expect } from "vitest")
    - Import `cn` from `@/lib/utils`
    - Test cases:
      1. `"merges class strings"`: `cn("px-2", "py-1")` returns `"px-2 py-1"`
      2. `"resolves conflicting Tailwind classes via tailwind-merge"`: `cn("px-2", "px-4")` returns `"px-4"`
      3. `"handles conditional classes via clsx"`: `cn("base", false && "hidden", "visible")` returns `"base visible"`
      4. `"handles empty input"`: `cn()` returns `""`
      5. `"removes falsy values"`: `cn("a", undefined, "b", null, "c")` returns `"a b c"`

    Use AAA pattern (Arrange-Act-Assert) per common/testing.md. No mocking needed — this tests the real implementation.
  </action>
  <verify>
    <automated>npx vitest run src/lib/utils.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 5 cn() test cases pass. Utility correctly merges, deduplicates, handles conditionals, and handles empty/falsy input.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write component tests for Button and Badge</name>
  <files>src/components/ui/button.test.tsx, src/components/ui/badge.test.tsx</files>
  <action>
    Create two component test files colocated with their source.

    **button.test.tsx** per RESEARCH.md Pattern test code:
    - Suite: `describe("Button", ...)`
    - Import: `{ render, screen }` from `@testing-library/react`, `userEvent` from `@testing-library/user-event`, `{ describe, it, expect, vi }` from `vitest`, `Button` from `@/components/ui/button`
    - Test cases:
      1. `"renders children text"`: render with "Click me" child, expect `getByRole("button")` to have textContent "Click me"
      2. `"calls onClick when clicked"`: vi.fn() handler, userEvent.click, expect onClick toHaveBeenCalledOnce()
      3. `"does not call onClick when disabled"`: Button with disabled prop, click, expect onClick not.toHaveBeenCalled()
      4. `"applies variant class correctly"`: render with `variant="destructive"`, check for attribute `data-slot="button"` (shadcn uses data-slot)
    - Use `userEvent` (not `fireEvent`) per RESEARCH.md anti-patterns (closer to real browser events).

    **badge.test.tsx**:
    - Suite: `describe("Badge", ...)`
    - Import: as above, `Badge` from `@/components/ui/badge`
    - Test cases:
      1. `"renders children"`: render `<Badge>New</Badge>`, expect `getByText("New")` to be in document
      2. `"applies default variant"`: render `<Badge>Default</Badge>`, verify it renders without error
      3. `"applies secondary variant"`: render `<Badge variant="secondary">Secondary</Badge>`, verify it renders
    - Badge uses CVA (class-variance-authority) for variants — test that different variants render without error.

    No need to test every variant exhaustively — the CVA library handles class resolution; these tests verify the component renders with expected variant props.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/button.test.tsx src/components/ui/badge.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>
    All Button test cases pass (render, click, disabled, variant). All Badge test cases pass (render, default variant, secondary variant).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code -> production code | Test files import production components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | Test imports | accept | Test files are development-only; never deployed |
</threat_model>

<verification>
- `npx vitest run --coverage` passes with coverage thresholds
- All 3 test files colocated per D-05
- Fixtures at src/__fixtures__/ per D-06
</verification>

<success_criteria>
10+ test cases across 3 test files. All green. Coverage ≥ 80% for src/lib/ (just counting utils.ts).
</success_criteria>

<output>
Create `.planning/phases/01.7-polish-deploy/01.7-03-SUMMARY.md` when done
</output>
