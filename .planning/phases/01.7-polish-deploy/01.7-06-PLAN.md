---
phase: 01.7-polish-deploy
plan: 06
type: execute
wave: 3
depends_on: [01.7-01, 01.7-02, 01.7-03, 01.7-04, 01.7-05]
files_modified: []
autonomous: false
requirements: [NFR1, NFR2, NFR3, NFR4, NFR6]
user_setup:
  - service: Formspree
    why: "Pre-flight verification of contact form submission"
    env_vars: []  # Formspree endpoint already configured in Phase 1.6
    dashboard_config:
      - task: "After deploy, submit test message via the live contact form"
        location: "https://tonyhyge.github.io/Tony-portfolio/ -> Contact section -> submit message"
        expected: "Check minhphamtony@gmail.com inbox for the test submission"

must_haves:
  truths:
    - "GitHub Actions workflow runs end-to-end on push to main"
    - "Build succeeds and deploys to tonyhyge.github.io/Tony-portfolio"
    - "404 page renders at unknown paths on the live site"
    - "robots.txt and sitemap.xml are accessible on the live site"
    - "Contact form submission reaches minhphamtony@gmail.com (Formspree pre-flight)"
  artifacts: []
  key_links: []
---
<objective>
Execute the first production deploy by pushing main and monitoring the GitHub Actions pipeline. Then perform pre-flight verification: smoke-test the live site, confirm 404/Sitemap/Robots endpoints, and verify Formspree form submission reaches the configured email.

Purpose: Go-live. The site is feature-complete after Phase 1.1 through 1.6. This plan makes it publicly accessible on GitHub Pages, verifies everything works, and completes the phase.
Output: Live site at tonyhyge.github.io/Tony-portfolio, verified end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.7-polish-deploy/01.7-CONTEXT.md
@.github/workflows/deploy.yml
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Ready to initiate first production deploy?</decision>
  <context>
    All testing infrastructure is configured, test files written, CI pipeline enhanced. This is the final step: commit, push to main, and monitor the GitHub Actions workflow.

    Before proceeding, ensure:
    - All local branches are clean
    - No pending uncommitted work
    - The Formspree endpoint in ContactForm.tsx is correct (already configured in Phase 1.6)
  </context>
  <options>
    <option id="deploy-now">
      <name>Deploy now</name>
      <pros>Site goes live immediately; earliest feedback</pros>
      <cons>None — all prerequisites met</cons>
    </option>
    <option id="defer-deploy">
      <name>Defer deploy</name>
      <pros>More time to review everything</pros>
      <cons>Delays go-live; no production feedback loop</cons>
    </option>
  </options>
  <resume-signal>Select: deploy-now or defer-deploy</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Commit all phase work and push to main</name>
  <files>.github/workflows/deploy.yml, vitest.config.ts, vitest.setup.ts, playwright.config.ts, lighthouserc.json, src/app/not-found.tsx, src/app/robots.ts, src/app/sitemap.ts, src/lib/utils.test.ts, src/components/ui/button.test.tsx, src/components/ui/badge.test.tsx, src/__fixtures__/index.ts, tests/e2e/portfolio.spec.ts, tests/e2e/responsive.spec.ts, tests/e2e/a11y.spec.ts</files>
  <action>
    If the user selected "deploy-now":

    1. Stage all new files:
       ```bash
       git add vitest.config.ts vitest.setup.ts playwright.config.ts lighthouserc.json
       git add src/app/not-found.tsx src/app/robots.ts src/app/sitemap.ts
       git add src/lib/utils.test.ts src/components/ui/button.test.tsx src/components/ui/badge.test.tsx
       git add src/__fixtures__/index.ts
       git add tests/e2e/
       git add .github/workflows/deploy.yml
       git add package.json
       ```
    2. Commit with message: `feat(01.7): testing infrastructure, 404 page, CI gates, go-live`
    3. Push to main: `git push origin main`
    4. Monitor the GitHub Actions run URL that appears in push output
    5. Wait for the workflow to complete successfully (poll with `gh run list --limit 1 --workflow deploy.yml` or `gh run watch`)
  </action>
  <verify>
    <automated>gh run list --limit 1 --json conclusion,status 2>/dev/null | grep -q '"success"' && echo "deploy succeeded" || echo "check status"</automated>
  </verify>
  <done>
    All phase files committed and pushed. GitHub Actions deploy workflow completes successfully (test -> build -> deploy -> audit all green).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Production deployment to GitHub Pages</what-built>
  <how-to-verify>
    1. Visit https://tonyhyge.github.io/Tony-portfolio/
    2. Verify the site loads with all sections: Hero, About, Experience, Research, Skills, Contact
    3. Test the 404 page: visit https://tonyhyge.github.io/Tony-portfolio/some-random-page
       - Confirm hexagon with "404" is visible
       - Confirm "Lost in the Lab?" heading is displayed
       - Click "Back to Home" and verify it navigates back to the main page
    4. Check robots.txt: https://tonyhyge.github.io/Tony-portfolio/robots.txt
       - Confirm it shows rules for all crawlers including GPTBot, ClaudeBot, PerplexityBot, OAI-SearchBot
       - Confirm it points to sitemap.xml at the production URL
    5. Check sitemap.xml: https://tonyhyge.github.io/Tony-portfolio/sitemap.xml
       - Confirm it has a home page entry and research project entries
    6. (Formspree pre-flight per D-25) Fill in the contact form with a test message and submit
       - Confirm the form submits without error
       - Check minhphamtony@gmail.com inbox for the test submission (allow a few minutes for delivery)
    7. Verify Lighthouse scores if you can run a Lighthouse test (optional for verified CI gate)
  </how-to-verify>
  <resume-signal>Type "approved" — site verified live, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git push -> GitHub Actions | CI triggers on push to main |
| Live site -> public internet | Publicly accessible portfolio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Spoofing | Formspree form submission | accept | Formspree handles spam filtering server-side; form uses client-side Zod validation |
| T-17-08 | Tampering | GitHub Actions workflow | mitigate | Workflow file checked into repo; changes tracked via git |
</threat_model>

<verification>
- Live site accessible at production URL
- 404 page renders correctly on the live domain
- robots.txt and sitemap.xml accessible
- Formspree pre-flight: test message arrives at minhphamtony@gmail.com
- GitHub Actions workflow shows all 4 jobs (test, build, deploy, audit) green
</verification>

<success_criteria>
Site is live at tonyhyge.github.io/Tony-portfolio. 404 page, robots.txt, sitemap.xml all serve correctly. Contact form submits successfully end-to-end.
</success_criteria>

<output>
Create `.planning/phases/01.7-polish-deploy/01.7-06-SUMMARY.md` when done
</output>
