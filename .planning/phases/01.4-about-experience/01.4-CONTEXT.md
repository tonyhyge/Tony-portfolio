# Phase 1.4: About & Experience — Context

**Gathered:** 2026-06-20
**Status:** Ready for planning

<domain>
## Phase Boundary

About section (personal narrative bio) + Experience timeline (professional journey) on the single-page portfolio. Includes: bio card with narrative paragraph and stats block, connected timeline component for experience entries, tech tags per entry, and data structure updates. Does NOT include: research projects (Phase 1.5), skills section (Phase 1.6), or the profile photo placeholder (already in Hero).

</domain>

<decisions>
## Implementation Decisions

### About Section Layout
- **D-01:** Split layout — narrative paragraph (left) + small stats block (right). Stats show: years of experience, lab affiliation, focus areas.
- **D-02:** Narrative tells the story: AI Student → AI Engineer → Researcher. Written as a 3-4 sentence personal narrative paragraph.
- **D-03:** Strictly professional — no personal interests or hobbies.
- **D-04:** No decorative imagery — text + stats only. The hero hexagon badge already provides visual interest above.

### Stats Block Content
- **D-05:** Three stats displayed: "3+ years experience", "AI Research Lab", "NLP & ML Systems". Rendered as a compact grid or row with small visual treatment (numbers prominent, labels subtle).

### Experience Timeline
- **D-06:** Hybrid connected timeline — vertical line with accent-colored dot nodes, but content blocks are styled as simple content panels (NOT full Card components with card header/footer pattern). Keeps the timeline feel without over-wrapping.
- **D-07:** Reverse chronological order — most recent role first (AI Engineer → Research Intern).
- **D-08:** Two entries for now: AI Engineer (current) and Research Intern. More can be added later via the data file.

### Tech Tags
- **D-09:** Tags appear as a Badge row below the description text (not inline in the header).
- **D-10:** Flat color — all tags use the same badge variant. No category-based color coding.

### Experience Data Structure
- **D-11:** Update `Experience` interface in `src/app/data/experience.ts`:
  - Keep existing fields: `role`, `company`, `location`, `startDate`, `endDate`, `description`, `highlights`
  - Add: `techTags: string[]`
  - Add optional: `companyUrl?: string`

### Section Integration
- **D-12:** About section renders in `#about` (already in `page.tsx`), uses Section component with "About" heading.
- **D-13:** Experience section renders in `#experience`, uses Section component with "Experience" heading.
- **D-14:** The existing `experience.ts` data file is consumed directly by the timeline component.

### Claude's Discretion
- Stats block exact layout (row vs grid, icon decoration)
- Timeline dot size, line thickness, and exact positioning (left vs center)
- Content block padding and hover effect (if any)
- Badge variant selection (default/outline/secondary) for tech tags — choose what matches the design system
- Exact spacing between timeline entries
- Responsive behavior — how the timeline collapses on mobile (dot on left, or simplified card list)

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Requirements & Roadmap
- `.planning/REQUIREMENTS.md` §F2 — About Section requirements (narrative, highlights, personal interests optional)
- `.planning/REQUIREMENTS.md` §F3 — Experience Timeline requirements (timeline view, company, role, duration, description, tech tags)
- `.planning/ROADMAP.md` §Phase 1.4 — Phase scope: about section, experience timeline, data structure

### Prior Phase Context
- `.planning/phases/11-project-scaffold-design-system/11-CONTEXT.md` — Design tokens, component architecture, data patterns (experience data as TypeScript objects), section order
- `.planning/phases/12-navigation-shell/12-CONTEXT.md` — Component patterns, Section component usage, established layout
- `.planning/phases/13-hero-section/13-CONTEXT.md` — Hero decisions that affect content below (style direction, gradient palette)

### Existing Code
- `src/app/data/experience.ts` — Current Experience interface + placeholder data (needs `techTags` + `companyUrl` added)
- `src/app/page.tsx` — Home page with `#about` and `#experience` placeholder sections
- `src/components/section.tsx` — Reusable Section wrapper with heading pattern
- `src/components/ui/badge.tsx` — Badge component for tech tags
- `src/components/ui/card.tsx` — Card component (not used directly for timeline but available)
- `src/components/container.tsx` — Max-width container for content alignment

### Design Assets
No external design files or specs — all decisions captured above.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- **Section** (`src/components/section.tsx`) — Ready-to-use with `id`, `title`, `subtitle` props. Both About and Experience sections use this.
- **Badge** (`src/components/ui/badge.tsx`) — Available for tech tags on experience entries. Has 5 variants.
- **Container** (`src/components/container.tsx`) — Already used by Section component, content aligns with hero and other sections.
- **experience.ts** (`src/app/data/experience.ts`) — Interface exists; needs `techTags` and optional `companyUrl` added. Two placeholder entries already present.

### Established Patterns
- **Data files as TypeScript objects** — experience, research, skills all follow this pattern (from Phase 1.1 CONTEXT.md)
- **Section + Container pattern** — Every section uses Section wrapper with Container inside (established in Phase 1.2)
- **Tailwind utility-first styling** — No CSS modules needed
- **"use client" pattern** — Not needed unless timeline has scroll-triggered animations (deferred to Phase 4.1)

### Integration Points
- **page.tsx** — `#about` and `#experience` sections already exist as placeholders with `className="min-h-screen"`
- **experience.ts** — Data file needs interface update; populate with real AI Engineer and Research Intern entries
- **NavLinks** — `#about` and `#experience` anchors already configured; smooth scroll targeting works

### New Components Needed
- `src/components/about/` directory (per web/coding-style.md feature-based organization)
  - `About.tsx` — Main About section composing narrative + stats
  - `BioCards.tsx` or inline — Narrative paragraph + stats block
- `src/components/experience/` directory
  - `ExperienceTimeline.tsx` — Timeline layout (vertical line + dot nodes + content blocks)
  - `ExperienceEntry.tsx` — Individual entry (role, company, dates, description, tech tags)

</code_context>

<specifics>
## Specific Ideas

- **Stats block:** Compact row of 3 stat items. Each stat: large number (or bold label) + subtle description below. Arranged horizontally on desktop, could wrap on mobile.
- **Timeline dots:** Use accent color (`--accent`) for the dot nodes. Line could be a thin `border-l` or pseudo-element.
- **Content blocks:** Each entry shows role (bold heading), company + location (subtle), date range, description paragraph, then tech tags as Badge row below.
- **Entry sections could be:** Title area (role | company · location | dates), Description paragraph, Tech tags row.

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: about-experience*
*Context gathered: 2026-06-20*
