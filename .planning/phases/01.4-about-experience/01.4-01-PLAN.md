---
phase: 01.4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/data/experience.ts
  - src/components/about/bio-narrative.tsx
  - src/components/about/stats-block.tsx
  - src/components/about/about-content.tsx
autonomous: true
requirements:
  - F2
  - F3
user_setup: []

must_haves:
  truths:
    - "About section shows personal narrative (AI Student -> AI Engineer -> Researcher)"
    - "Stats block displays 3 items: years experience, lab affiliation, focus area"
    - "Stats block has NO decorative imagery -- text and values only"
    - "Experience data interface includes techTags and companyUrl fields"
    - "Experience data has two real entries (AI Engineer current + Research Intern past)"
    - "No personal interests or hobbies appear in the narrative"
  artifacts:
    - path: "src/app/data/experience.ts"
      provides: "Updated Experience interface and populated data array"
      contains: "techTags: string[]"
      contains: "companyUrl?: string"
      contains: '"AILOGY"'
      contains: '"Research Intern"'
    - path: "src/components/about/bio-narrative.tsx"
      provides: "Narrative paragraph component"
      exports: ["BioNarrative"]
      min_lines: 10
    - path: "src/components/about/stats-block.tsx"
      provides: "Three-stat compact display"
      exports: ["StatsBlock"]
      contains: '"3+"'
      contains: '"Years"'
      contains: '"AI"'
    - path: "src/components/about/about-content.tsx"
      provides: "Split layout composing BioNarrative + StatsBlock"
      exports: ["AboutContent"]
      contains: "BioNarrative"
      contains: "StatsBlock"
      contains: "grid gap-8 md:grid-cols-\\[1fr_auto\\]"
  key_links:
    - from: "src/components/about/about-content.tsx"
      to: "src/components/about/bio-narrative.tsx"
      via: "import and compose"
      pattern: "import.*BioNarrative"
    - from: "src/components/about/about-content.tsx"
      to: "src/components/about/stats-block.tsx"
      via: "import and compose"
      pattern: "import.*StatsBlock"
    - from: "src/app/data/experience.ts"
      to: "ExperienceEntry consumer (Plan 02)"
      via: "exports interface and data array"
      pattern: "export const experiences"
---

<objective>
Update the Experience data model and build the About section components.

Purpose: Extend the Experience interface with techTags and companyUrl, populate the data array with real entries for both roles (AI Engineer at AILOGY, Research Intern), and create three About section components: narrative paragraph (BioNarrative), three-stat display (StatsBlock), and their composition (AboutContent).

Output: Modified experience.ts with populated data, three new about components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
Existing types consumed by this plan:

From src/app/data/experience.ts:
```typescript
export interface Experience {
  role: string;
  company: string;
  location: string;
  startDate: string;
  endDate: string | "Present";
  description: string;
  highlights: string[];
  // ADD: techTags: string[];
  // ADD: companyUrl?: string;
}
```

Badge component (src/components/ui/badge.tsx) -- 5 variants: default, secondary, destructive, outline, ghost, link
Section component (src/components/section.tsx) -- accepts id, title, subtitle, className, children
Container component (src/components/container.tsx) -- accepts className, maxWidth ("sm"|"md"|"lg"|"xl"|"2xl"|"full")
</interfaces>

**Locked decisions being implemented:**
- D-02: Narrative tells AI Student -> AI Engineer -> Researcher story, 3-4 sentences
- D-03: Strictly professional -- no personal interests or hobbies
- D-04: No decorative imagery -- text + stats only
- D-05: Three stats: "3+ years experience", "AI Research Lab", "NLP & ML Systems" -- numbers prominent, labels subtle
- D-11: Add techTags: string[] (required) and companyUrl?: string (optional) to Experience interface
- D-07: Reverse chronological order (AI Engineer first, then Research Intern)
- D-08: Two entries: AI Engineer (current), Research Intern

**From PATTERNS.md:**
- Feature directory pattern: src/components/about/ with PascalCase files
- Data file pattern: interface at top, exported const array below
- Component export: `export function ComponentName()`, no React.FC
- Stats layout: grid-cols-3 with text-accent for values, text-muted-foreground for labels
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Experience data model with techTags and populate real entries</name>
  <files>src/app/data/experience.ts</files>
  <read_first>
    - src/app/data/experience.ts (current state -- interface needs techTags + companyUrl)
    - .planning/phases/01.4-about-experience/01.4-CONTEXT.md (D-11, D-07, D-08)
  </read_first>
  <action>
    Modify src/app/data/experience.ts:

    1. Add two fields to the Experience interface:
       - `techTags: string[];` (required)
       - `companyUrl?: string;` (optional)

    2. Replace the entire experiences array with two real entries in reverse chronological order (D-07):

    First entry (current role -- AI Engineer):
    - role: "AI Engineer"
    - company: "AILOGY"
    - location: "Hanoi, Vietnam" (or "Remote" if preferred)
    - startDate: "2024-07"
    - endDate: "Present"
    - description: "Building ML-powered products and AI solutions for production environments. Design, train, and deploy machine learning models serving thousands of users."
    - highlights: 3 bullet points about production ML pipelines, LLM fine-tuning, inference optimization
    - techTags: ["Python", "PyTorch", "LangChain", "RAG", "LoRA", "FastAPI", "Docker"]
    - companyUrl: "https://ailogy.com" (or omit if unknown)

    Second entry (research intern):
    - role: "Research Intern"
    - company: "AI Research Lab" -- keep as "University Lab" for affiliation
    - location: "University Lab"
    - startDate: "2023-06"
    - endDate: "2024-05"
    - description: "Conducted research in natural language processing and representation learning, with a focus on efficient transformer architectures and low-resource language understanding."
    - highlights: 3 bullet points about paper publication, evaluation benchmarks, baseline implementations
    - techTags: ["Python", "PyTorch", "Transformers", "NLP", "Vietnamese NLP", "BERT"]
    - companyUrl: omit (no URL)

    Keep the existing export pattern: `export interface Experience { ... }` and `export const experiences: Experience[] = [ ... ]`.

    No import changes needed. No other files affected.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    1. File compiles: `npm run build` exits 0
    2. Interface includes `techTags: string[]` -- grep for `techTags: string[]` in experience.ts
    3. Interface includes `companyUrl?: string` -- grep for `companyUrl?: string` in experience.ts
    4. First entry has role "AI Engineer" at "AILOGY" -- grep for 'role: "AI Engineer"' and '"AILOGY"'
    5. Second entry has role "Research Intern" -- grep for 'role: "Research Intern"'
    6. AI Engineer entry has non-empty techTags array with >3 entries -- check array has entries
    7. AI Engineer entry has companyUrl field -- grep for 'companyUrl'
    8. Research Intern entry has non-empty techTags array with >3 entries -- check array has entries
    9. Data array still exported -- grep for 'export const experiences'
  </acceptance_criteria>
  <done>Experience interface updated with techTags and companyUrl. Both data entries populated with real content. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create BioNarrative and StatsBlock components</name>
  <files>
    src/components/about/bio-narrative.tsx
    src/components/about/stats-block.tsx
  </files>
  <read_first>
    - src/components/hero/HeroCtas.tsx (analog pattern -- child presentational component)
  </read_first>
  <action>
    Create two files in src/components/about/:

    **bio-narrative.tsx:**
    - Export function `BioNarrative()` (no props -- static content)
    - No React.FC, no default export
    - Wrapper: `<div className="max-w-prose space-y-4">`
    - Two `<p>` elements with `className="text-base leading-relaxed text-foreground/80"`
    - Per D-02: Narrative tells the story AI Student -> AI Engineer -> Researcher in ~3-4 sentences total across two paragraphs.
    - Per D-03: Content is strictly professional. No hobbies, no personal interests.
    - Paragraph 1: Opens with AI student discovery of NLP/language models, transitions to AI Engineer building production systems (LLM fine-tuning, inference APIs).
    - Paragraph 2: Current work at intersection of engineering and research -- efficient transformer architectures, evaluation benchmarks, low-resource languages.
    - Make the text natural and personal, not generic. Reference specific areas (NLP, LLMs, production ML) that match the experience data.

    **stats-block.tsx:**
    - Export function `StatsBlock()` (no props -- data inline)
    - No React.FC, no default export
    - Per D-04: No decorative imagery. Text and values only. No lucide-react icons.
    - Per D-05: Three stats displayed in a compact grid:
      1. value: "3+", label: "Years of Experience"
      2. value: "AI Lab", label: "Research Affiliation"  
      3. value: "NLP/ML", label: "Focus Area"
    - Layout: `<div className="grid grid-cols-3 gap-4 sm:gap-6 self-start">`
    - Each stat: value as `<div className="text-2xl font-bold text-accent sm:text-3xl">`, label as `<div className="mt-1 text-xs text-muted-foreground sm:text-sm">`
    - Render via mapping over a stats array defined inside the function

    Both files follow the established patterns from PATTERNS.md (PascalCase filenames, export function, no React.FC, Tailwind utilities only).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    1. `npm run build` exits 0
    2. bio-narrative.tsx exports `BioNarrative` function -- grep for 'export function BioNarrative' in bio-narrative.tsx
    3. bio-narrative.tsx has no default export -- grep for 'export default' should not find it
    4. bio-narrative.tsx contains two `<p>` elements with text about AI journey -- verify visually
    5. bio-narrative.tsx has no personal interests/hobbies content -- scan text
    6. stats-block.tsx exports `StatsBlock` function -- grep for 'export function StatsBlock' in stats-block.tsx
    7. stats-block.tsx contains "3+" -- grep for '"3\\+"' or `3+`
    8. stats-block.tsx contains "AI Lab" -- grep for 'AI Lab'
    9. stats-block.tsx contains "NLP/ML" -- grep for 'NLP/ML'
    10. stats-block.tsx has NO lucide-react imports -- grep for 'lucide-react' should fail
    11. stats-block.tsx has NO image/img tags -- grep for '<img\\|Image\\|icon' (case insensitive check)
    12. Both files use `export function` pattern, not `export default function` -- verify
  </acceptance_criteria>
  <done>BioNarrative and StatsBlock components created with correct content, no decorative imagery, no personal interests.</done>
</task>

<task type="auto">
  <name>Task 3: Create AboutContent composition component</name>
  <files>src/components/about/about-content.tsx</files>
  <read_first>
    - src/components/about/bio-narrative.tsx (just created in Task 2)
    - src/components/about/stats-block.tsx (just created in Task 2)
    - src/components/hero/Hero.tsx (analog pattern -- composition parent)
  </read_first>
  <action>
    Create src/components/about/about-content.tsx:

    - Export function `AboutContent()` (no props -- data fetched internally via imports)
    - No React.FC, no default export
    - Import BioNarrative from `./bio-narrative`
    - Import StatsBlock from `./stats-block`
    - Per D-01: Split layout -- narrative paragraph (left), stats block (right).
    - Layout: `<div className="grid gap-8 md:grid-cols-[1fr_auto] md:gap-12 lg:gap-16">`
      - First child: `<BioNarrative />`
      - Second child: `<StatsBlock />`
    - The grid layout gives the narrative `1fr` (fills remaining space) and the stats block `auto` (its natural width).
    - On mobile (< 768px), the single column stacks BioNarrative above StatsBlock.
    - No Section wrapper here -- Section wrapper is applied in page.tsx (Plan 03).

    Follow the established feature directory pattern from PATTERNS.md.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    1. `npm run build` exits 0
    2. File exports `AboutContent` -- grep for 'export function AboutContent'
    3. File imports BioNarrative -- grep for 'import.*BioNarrative'
    4. File imports StatsBlock -- grep for 'import.*StatsBlock'
    5. Layout contains `md:grid-cols-\[1fr_auto\]` -- grep for this exact pattern
    6. No Section wrapper used -- grep for 'Section' should not find it
    7. Uses `export function AboutContent` pattern, not default export -- verify
  </acceptance_criteria>
  <done>AboutContent composition component created with split layout. Imports and composes BioNarrative + StatsBlock.</done>
</task>

</tasks>

<threat_model>
## Threat Model

This plan creates static content components. No interactive elements, no user input, no API calls.

| Boundary | Description |
|----------|-------------|
| None | Static rendering at build time; no runtime trust boundaries |

**STRIDE:** No applicable threats -- all content is compile-time TypeScript objects and pre-authored JSX strings. No injection surface, no authentication, no network I/O.
</threat_model>

<verification>
- Task 1 verification: `npm run build` passes, grep checks for interface fields and data content
- Task 2 verification: `npm run build` passes, grep checks for component exports and content
- Task 3 verification: `npm run build` passes, grep checks for imports and layout pattern
</verification>

<success_criteria>
- [ ] Experience interface has techTags: string[] and companyUrl?: string
- [ ] Experiences array has two real entries: AI Engineer at AILOGY, Research Intern
- [ ] Both entries have populated techTags arrays
- [ ] BioNarrative component renders professional-only narrative text
- [ ] StatsBlock component shows 3 stats with no icons/images
- [ ] AboutContent composes BioNarrative + StatsBlock in grid split layout
- [ ] All new files follow established component patterns (no React.FC, named export, PascalCase)
</success_criteria>

<output>
After completion, create `.planning/phases/01.4-about-experience/01.4-01-SUMMARY.md`
</output>
