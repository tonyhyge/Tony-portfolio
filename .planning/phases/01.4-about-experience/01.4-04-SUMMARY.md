---
phase: 01.4
plan: 04
subsystem: experience
tags: [dual-track, concurrent-roles, experience-data, hideLine]
requires: []
provides:
  - Experience interface with track + groupId fields
  - Concurrent Research Assistant entry (research track)
  - hideLine prop on ExperienceEntry
affects:
  - src/app/data/experience.ts
  - src/components/experience/experience-entry.tsx
tech-stack:
  added: []
  patterns:
    - Optional discriminated union fields (track?: "industry" | "research")
    - Conditional early return for hideLine rendering mode
key-files:
  created: []
  modified:
    - src/app/data/experience.ts
    - src/components/experience/experience-entry.tsx
key-decisions:
  - "D-11: track and groupId added to Experience interface"
  - "D-15: groupId groups concurrent entries; entries without render as singles"
  - "D-19: hideLine prop on ExperienceEntry suppresses dot/connector for dual-track mode"
  - "D-22: Both tracks use same accent-colored dot (no per-track diff)"
requirements-completed: [F3]
---

# Phase 01.4 Plan 04: Concurrent Dual-Track Data Model — Summary

**One-liner:** Extended Experience interface with track+groupId, added concurrent Research Assistant entry in research track, added hideLine prop to ExperienceEntry for dual-track rendering support.

## Files Modified

| File | Status | Purpose |
|------|--------|---------|
| `src/app/data/experience.ts` | MODIFIED | Added track/groupId to interface, Research Assistant entry, tagged AI Engineer |
| `src/components/experience/experience-entry.tsx` | MODIFIED | Added optional hideLine prop with content-panel-only rendering |

## Tasks Executed

| Task | Files |
|------|-------|
| 1. Add track and groupId fields to Experience interface | experience.ts |
| 2. Add Research Assistant concurrent entry + update AI Engineer | experience.ts |
| 3. Add hideLine prop to ExperienceEntry | experience-entry.tsx |

## Key Technical Details

### Data Model Changes
- Added `track?: "industry" | "research"` — discriminates visual treatment for concurrent roles
- Added `groupId?: string` — groups concurrent entries into shared time-period row
- AI Engineer tagged with `track: "industry"`, `groupId: "concurrent-1"`
- New Research Assistant entry with `track: "research"`, `groupId: "concurrent-1"`, same startDate "2024-07"
- Research Intern entry unchanged (no track/groupId — backward compatible)
- 3 entries total in data array

### hideLine Prop
- Optional `hideLine?: boolean` with default `false` (backward compatible)
- When `true`: renders content-panel-only (no dot, no connector, no `relative pl-8` wrapper)
- All existing `<ExperienceEntry>` usages without `hideLine` render unchanged

## Deviations from Plan

None — plan executed exactly as written.

## Verification

- [x] Build passes (TypeScript compiled, static pages generated)
- [x] Interface has `track?: "industry" | "research"` and `groupId?: string`
- [x] AI Engineer has `track: "industry"`, `groupId: "concurrent-1"`
- [x] Research Assistant has `track: "research"`, `groupId: "concurrent-1"`
- [x] Research Intern has no track/groupId
- [x] hideLine prop exists with default false
- [x] hideLine=true path renders without dot/connector
- [x] All existing ExperienceEntry usages preserved
