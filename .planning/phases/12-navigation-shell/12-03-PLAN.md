---
phase: 12
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
  - 12-02
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: true
requirements: [F9, F10, NFR2, NFR3]

must_haves:
  truths:
    - layout.tsx wraps children with LenisProvider, is wrapped by SiteHeader and SiteFooter
    - Section placeholders exist in page.tsx with matching IDs for IntersectionObserver targeting
    - Active section tracking works because every section in the nav has a corresponding <section id=""> in page.tsx
  artifacts:
    - path: src/app/layout.tsx
      provides: Integrated layout with SiteHeader, LenisProvider, SiteFooter
      exports: ["default"]
      contains: "SiteHeader"
    - path: src/app/page.tsx
      provides: Section placeholders with IDs matching navLinks anchor hrefs
      contains: "section"
  key_links:
    - from: src/app/layout.tsx
      to: src/components/site-header.tsx
      via: "import { SiteHeader } from"
    - from: src/app/layout.tsx
      to: src/components/lenis-provider.tsx
      via: "import { LenisProvider } from"
    - from: src/app/layout.tsx
      to: src/components/site-footer.tsx
      via: "import { SiteFooter } from"
---

<objective>
Layout integration: wire SiteHeader, LenisProvider, and SiteFooter into layout.tsx, add section placeholders to page.tsx.

Purpose: Close the loop — integrate all components into the app shell so the scroll navigation and header/footer function as a complete system.

Output: Updated layout.tsx with LenisProvider wrapping children, SiteHeader at top, SiteFooter at bottom. Updated page.tsx with minimal placeholder sections that have IDs matching nav anchor links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/layout.tsx (current root layout)
@src/app/page.tsx (current home page shell)
@src/components/site-header.tsx (from Plan 02)
@src/components/site-footer.tsx (from Plan 01)
@src/components/lenis-provider.tsx (from Plan 02)
@src/app/data/navigation.ts (navLinks — section IDs for placeholders)
@.planning/phases/12-navigation-shell/12-UI-SPEC.md (responsive contract)

<interfaces>
From navigation.ts:
```typescript
export const navLinks: NavLink[] = [
  { label: "Home", href: "#home", isAnchor: true },
  { label: "About", href: "#about", isAnchor: true },
  { label: "Experience", href: "#experience", isAnchor: true },
  { label: "Research", href: "/research", isAnchor: false },
  { label: "Skills", href: "#skills", isAnchor: true },
  { label: "Blog", href: "/blog", isAnchor: false },
  { label: "Contact", href: "#contact", isAnchor: true },
];
```

From site-header.tsx:
```typescript
export function SiteHeader() { ... }  // "use client" component
```

From site-footer.tsx:
```typescript
export function SiteFooter() { ... }  // no "use client" directive (static)
```

From lenis-provider.tsx:
```typescript
export function LenisProvider({ children }: { children: React.ReactNode }) { ... }  // "use client" component
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add section placeholders to page.tsx</name>

  <read_first>
    /Users/minhpham/Claude/Projects/personal portfolio/src/app/page.tsx (current state — comments only)
    /Users/minhpham/Claude/Projects/personal portfolio/src/app/data/navigation.ts (section IDs to match)
    /Users/minhpham/Claude/Projects/personal portfolio/src/components/section.tsx (existing Section component for reference — not required for this simple task)
  </read_first>

  <files>
    /Users/minhpham/Claude/Projects/personal portfolio/src/app/page.tsx
  </files>

  <action>
    Replace the content of `/Users/minhpham/Claude/Projects/personal portfolio/src/app/page.tsx` with minimal placeholder sections that have matching IDs.

    Per D-10 nav order (Home, About, Experience, Research, Skills, Contact) but excluding page-based sections (Research and Blog — they route to `/research` and `/blog` pages). The `page.tsx` sections should only include anchor sections: Home, About, Experience, Skills, Contact.

    ```tsx
    export default function Home() {
      return (
        <>
          <section
            id="home"
            className="min-h-screen flex items-center justify-center"
            aria-label="Home"
          >
            {/* HERO — Phase 1.3 */}
          </section>

          <section
            id="about"
            className="min-h-screen"
            aria-label="About"
          >
            {/* ABOUT — Phase 1.4 */}
          </section>

          <section
            id="experience"
            className="min-h-screen"
            aria-label="Experience"
          >
            {/* EXPERIENCE — Phase 1.4 */}
          </section>

          <section
            id="skills"
            className="min-h-screen"
            aria-label="Skills"
          >
            {/* SKILLS — Phase 1.6 */}
          </section>

          <section
            id="contact"
            className="min-h-screen"
            aria-label="Contact"
          >
            {/* CONTACT — Phase 1.6 */}
          </section>
        </>
      );
    }
    ```

    Key details:
    1. 5 sections matching anchor nav links: `#home`, `#about`, `#experience`, `#skills`, `#contact`
    2. Research (`/research`) and Blog (`/blog`) are page links, not anchor sections — NOT included
    3. Home section uses `min-h-screen flex items-center justify-center` (hero will be centered)
    4. All sections use `min-h-screen` so IntersectionObserver has actual viewport-height elements to observe (per RESEARCH.md Open Question 3)
    5. `aria-label` on each section for screen reader identification (per NFR2)
    6. Preserves comment markers for future phases (/* Phase 1.3 */, /* Phase 1.4 */, etc.)
  </action>

  <verify>
    <automated>
      grep -c '<section' /Users/minhpham/Claude/Projects/personal portfolio/src/app/page.tsx | xargs -I{} echo "sections count: {}"
      grep -q 'id="home"' /Users/minhpham/Claude/Projects/personal portfolio/src/app/page.tsx && echo "home section: OK" || echo "home section: MISSING"
      grep -q 'id="about"' /Users/minhpham/Claude/Projects/personal portfolio/src/app/page.tsx && echo "about section: OK" || echo "about section: MISSING"
      grep -q 'id="experience"' /Users/minhpham/Claude/Projects/personal portfolio/src/app/page.tsx && echo "experience section: OK" || echo "experience section: MISSING"
      grep -q 'id="skills"' /Users/minhpham/Claude/Projects/personal portfolio/src/app/page.tsx && echo "skills section: OK" || echo "skills section: MISSING"
      grep -q 'id="contact"' /Users/minhpham/Claude/Projects/personal portfolio/src/app/page.tsx && echo "contact section: OK" || echo "contact section: MISSING"
      grep -q 'min-h-screen' /Users/minhpham/Claude/Projects/personal portfolio/src/app/page.tsx && echo "min-h-screen: OK" || echo "min-h-screen: MISSING"
      grep -q 'aria-label' /Users/minhpham/Claude/Projects/personal portfolio/src/app/page.tsx && echo "aria-label: OK" || echo "aria-label: MISSING"
      grep -q 'Phase 1\.[3-6]' /Users/minhpham/Claude/Projects/personal portfolio/src/app/page.tsx && echo "phase comments preserved: OK" || echo "phase comments: CHECK"
      # Verify no section for /research or /blog (those are page links)
      grep -q 'id="research"' /Users/minhpham/Claude/Projects/personal portfolio/src/app/page.tsx && echo "ERROR: research section should not exist" || echo "research page link correctly excluded: OK"
      grep -q 'id="blog"' /Users/minhpham/Claude/Projects/personal portfolio/src/app/page.tsx && echo "ERROR: blog section should not exist" || echo "blog page link correctly excluded: OK"
    </automated>
  </verify>

  <acceptance_criteria>
    - `page.tsx` contains exactly 5 `<section>` elements with IDs: `home`, `about`, `experience`, `skills`, `contact`
    - Each section has `min-h-screen` and `aria-label`
    - Research and Blog are NOT included as sections (they route to `/research` and `/blog` pages)
    - Phase comments preserved for future content phases
    - `npm run build` passes
  </acceptance_criteria>

  <done>
    Section placeholders created in page.tsx with correct IDs for IntersectionObserver targeting
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate SiteHeader, LenisProvider, SiteFooter into layout.tsx</name>

  <read_first>
    /Users/minhpham/Claude/Projects/personal portfolio/src/app/layout.tsx (current root layout)
    /Users/minhpham/Claude/Projects/personal portfolio/src/components/site-header.tsx (import target)
    /Users/minhpham/Claude/Projects/personal portfolio/src/components/site-footer.tsx (import target)
    /Users/minhpham/Claude/Projects/personal portfolio/src/components/lenis-provider.tsx (import target)
  </read_first>

  <files>
    /Users/minhpham/Claude/Projects/personal portfolio/src/app/layout.tsx
  </files>

  <action>
    Update `/Users/minhpham/Claude/Projects/personal portfolio/src/app/layout.tsx` to integrate the navigation shell.

    Replace the entire file with:

    ```tsx
    import type { Metadata } from "next";
    import { Inter, JetBrains_Mono } from "next/font/google";
    import { SiteHeader } from "@/components/site-header";
    import { SiteFooter } from "@/components/site-footer";
    import { LenisProvider } from "@/components/lenis-provider";
    import "./globals.css";

    const inter = Inter({
      subsets: ["latin"],
      variable: "--font-inter",
      display: "swap",
    });

    const jetbrainsMono = JetBrains_Mono({
      subsets: ["latin"],
      variable: "--font-jetbrains-mono",
      display: "swap",
    });

    export const metadata: Metadata = {
      title: "Tony Portfolio",
      description: "AI Engineer & Researcher",
    };

    const themeScript = `
          (function() {
            const theme = localStorage.getItem('theme');
            if (theme === 'dark' || (!theme && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
              document.documentElement.classList.add('dark');
            }
          })();
        `;

    export default function RootLayout({
      children,
    }: Readonly<{
      children: React.ReactNode;
    }>) {
      return (
        <html
          lang="en"
          className={`${inter.variable} ${jetbrainsMono.variable} antialiased`}
          suppressHydrationWarning
        >
          <head>
            <script dangerouslySetInnerHTML={{ __html: themeScript }} />
          </head>
          <body className="min-h-screen flex flex-col bg-background text-foreground">
            <LenisProvider>
              <SiteHeader />
              <main className="flex-1">{children}</main>
              <SiteFooter />
            </LenisProvider>
          </body>
        </html>
      );
    }
    ```

    Changes from current layout.tsx:

    1. Added imports: `SiteHeader`, `SiteFooter`, `LenisProvider`
    2. Wrapped `<body>` children with `<LenisProvider>` (provides smooth scroll engine globally)
    3. Added `<SiteHeader />` before `<main>` (sticky header on every page)
    4. Wrapped `{children}` in `<main className="flex-1">` (semantic main element per NFR2, `flex-1` pushes footer to bottom when content is short)
    5. Added `<SiteFooter />` after `<main>` (footer on every page)
    6. Changed `min-h-full` to `min-h-screen` on `<body>` for full-viewport column layout

    The LenisProvider wraps both SiteHeader and SiteFooter so Lenis handles scroll for the entire page including the header (required for hide-on-scroll-down to stay in sync with Lenis-driven scroll position). Other layout elements (html attributes, font loading, themeScript, metadata) remain unchanged.
  </action>

  <verify>
    <automated>
      grep -q "SiteHeader" /Users/minhpham/Claude/Projects/personal portfolio/src/app/layout.tsx && echo "SiteHeader imported: OK" || echo "SiteHeader: MISSING"
      grep -q "SiteFooter" /Users/minhpham/Claude/Projects/personal portfolio/src/app/layout.tsx && echo "SiteFooter imported: OK" || echo "SiteFooter: MISSING"
      grep -q "LenisProvider" /Users/minhpham/Claude/Projects/personal portfolio/src/app/layout.tsx && echo "LenisProvider imported: OK" || echo "LenisProvider: MISSING"
      grep -q "<SiteHeader />" /Users/minhpham/Claude/Projects/personal portfolio/src/app/layout.tsx && echo "SiteHeader rendered: OK" || echo "SiteHeader render: MISSING"
      grep -q "<SiteFooter />" /Users/minhpham/Claude/Projects/personal portfolio/src/app/layout.tsx && echo "SiteFooter rendered: OK" || echo "SiteFooter render: MISSING"
      grep -q "<LenisProvider>" /Users/minhpham/Claude/Projects/personal portfolio/src/app/layout.tsx && echo "LenisProvider rendered: OK" || echo "LenisProvider render: MISSING"
      grep -q '<main' /Users/minhpham/Claude/Projects/personal portfolio/src/app/layout.tsx && grep -q 'flex-1' /Users/minhpham/Claude/Projects/personal portfolio/src/app/layout.tsx && echo "main flex-1: OK" || echo "main flex-1: MISSING"
      grep -q "min-h-screen" /Users/minhpham/Claude/Projects/personal portfolio/src/app/layout.tsx && echo "min-h-screen: OK" || echo "min-h-screen: MISSING"
      grep -q "Inter" /Users/minhpham/Claude/Projects/personal portfolio/src/app/layout.tsx && echo "Inter font: OK" || echo "Inter font: MISSING"
      grep -q "JetBrains_Mono" /Users/minhpham/Claude/Projects/personal portfolio/src/app/layout.tsx && echo "JetBrains Mono: OK" || echo "JetBrains Mono: MISSING"
    </automated>
  </verify>

  <acceptance_criteria>
    - `layout.tsx` imports and renders `SiteHeader` before `<main>`, `SiteFooter` after `<main>`
    - `layout.tsx` imports and wraps content with `LenisProvider`
    - `{children}` wrapped in `<main className="flex-1">` (semantic main element)
    - Font configuration unchanged (Inter, JetBrains_Mono)
    - Theme flash prevention script unchanged
    - `suppressHydrationWarning` preserved on `<html>`
    - `npm run build` passes
  </acceptance_criteria>

  <done>
    Layout integrated — SiteHeader at top, SiteFooter at bottom, LenisProvider wraps all content
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | Static layout composition — no user input, no external data, no dynamic injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering | themeScript `dangerouslySetInnerHTML` | accept | Script is static, contains no user-controlled data. Already present in Phase 1.1 layout. Maintained unchanged. |
</threat_model>

<verification>
1. `npm run build` — static export compiles without errors
2. `npx tsc --noEmit --pretty false` — TypeScript type-checks pass
3. Check grep results match acceptance criteria
</verification>

<success_criteria>
- page.tsx has 5 section elements with matching anchor IDs (home, about, experience, skills, contact)
- layout.tsx integrates SiteHeader, LenisProvider, and SiteFooter
- Body uses `min-h-screen flex flex-col` with `flex-1` on main content
- Build compiles, TypeScript passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-navigation-shell/12-03-SUMMARY.md`
</output>
